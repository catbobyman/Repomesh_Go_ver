# 联调环境（CONS-11/12）：读模型 API 专用实例的开发凭据，非机密。
# base 留空 = 同源（经 vite proxy /api → 127.0.0.1:8100）。
VITE_API_BASE=
VITE_API_TOKEN=console-dev-token
# 治理决策主体：**默认从花名册派生**（该组织的 active organization_leader，
# 见 src/api/decisions.ts:resolveGovernanceAgent），此处留空即可。
#
# 原先写死的 d2abc576-… 已随 5533 DROP 重建重种失效（org leader 是 v4 随机 uuid，
# 每次重种都换）。留一个失效值在这里比没有更糟——它的优先级高于派生，会让派生
# 永不触发、真批照样 403。要指定特定主体时才填，填之前先确认它在花名册里。
VITE_GOVERNANCE_AGENT_ID=
