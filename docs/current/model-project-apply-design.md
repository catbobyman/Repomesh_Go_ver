# F04 模型用于项目 · 页面提案 r1

本次补充：原UI采用范围不变；此前列为未补的协议/恢复现已形成[六项完整待审稿](first-batch-complete-review.md)，尚未用户采用或真实实现。原文下方描述保留当轮评审边界。

2026-09-12一致性修复：串联原型的模型行与“用于项目”预览现在共同按供应商身份、不可变供应商版本和模型ID读取独立测试观察；测试通过／失败／未知不会写入模型配置，也不会成为允许应用的额外门槛。候选预览保留自己的快照身份，不因当前页切供应商或保存新版本而移用其他观察。新快照仍未测试，旧快照的原测试记录保留。本次修复已采用UI目标的显示矛盾，不将待审模型协议改为已采用；验证仍限本地脚本与原型。

用户“同意，继续下一项”采用上一页F03所列三项安排，随后进入F04；用户最新回复“同意，现在收拢范围，只做最关键基本的的几个页面，我要开始开发了。现在先给我一个待做表”，采用下列三项页面安排。主题RM-UI-MODEL-APPLY r1是原模型设置r3明确保留的未完交互，不重开旧r3，不改变现行configuration PATCH。

## 页面与待裁决安排

入口：主界面 → 模型设置 → 具体模型行“用于项目”。沿已采用供应商分栏页面及弹窗视觉，新增[原型片段](../prototypes/model-project-apply.prototype.js)由页面独占，生成器嵌入原模型页；原15份HTML不改。[串联展示](../prototypes/index.html#model-apply)只是原型场景定位。

已采用三项UI安排（不等于完整协议或实现完成）：

1. 弹窗先选有配置资格的项目，再展示“替换后模型”、“替换前模型”及“保持执行配置不变”。模型侧列供应商／模型ID、连接与模型参数固定快照、只读密钥版本标识和测试观察；当前模型仅为替换前对照，保持不变只指原execution引用来源及有效版本。项目修订单独标为预览／提交比较基线，不承诺应用后不变。密钥原文不展示。主要信息可见，参数详情折叠，代价是弹窗需要滚动。
2. 预览条件不完整或发生变化时暂停确认，说明待核实项／变化内容；重新读取后由用户再次明确确认，不自动采用新版本。执行配置原有效版本未知时不允许把“同一个引用”当作精确保留保证。
3. 提交结果未知时保留原应用入口，只核查该次结果；未查到仍未知，不能换键重发。离开弹窗可稍后从模型页继续核查；已确认未保存的冲突回读并重新确认，成功回执后再查看项目当前配置。

## 已采用目标与未定协议

唯一已有目标依据为[模型设置§3](model-connection-settings-design.md)：仅更换模型，精确保留execution原引用及有效版本；提交绑定预览模型固定快照与项目修订，变化需重新明确；其他项目和在途Issue不随之切换。[首批浏览器契约§5／7](first-batch-browser-api-contract.md)仍双引用重解析，不能拿其完整PATCH实现本页保证。

本页记录已采用的三项呈现，不新增REST端点、机器字段、SecretVersion格式、预览令牌、有效期、幂等键或错误码。上述机器协议现由[模型浏览器候选](model-settings-browser-api-draft.md)统一维护，仍待采用。原模型受限形状和固定来源定位尚有[字段缺口 C06](../reviews/2026-09-12-project-contracts/README.md#c06-应用预览缺少原模型的完整形状)，不能由本页样例自行补字段。

项目列表只来自本次预览内已建立的样例项目，不代表真实配置资格查询。完整预览中的项目修订、当前模型、执行版本和密钥版本均明确标记为示例观察，不从原型旧“开发容器v2”或供应商版本推断真实项目事实。另提供“预览信息缺失”场景展示禁止确认。测试未测试／失败不自动禁止保存待配置引用；保存成功不代表模型可用或执行Ready。

## 原型范围与未完成

原型内演示：完整样例预览、信息缺失、预览变化、提交中、确定冲突未保存、结果未知、查询仍未查到／模拟确认成功、回读当前样例。只在本片段内更新样例模型选择，不写真实配置，不让其样例execution版本覆盖F02显示的未知事实。F02与F03已采用范围保持。

具体配置资格／失权清理、版本或密钥撤销、历史固定快照可用性、结果清理／404／410的专用协议、请求晚到、跨主体／多标签／多操作／跨刷新恢复、真实测试与费用等未完成。当前原型不提供未知后的新应用入口、不提供原输入丢失后的重发，也不伪造已清理结果恢复。F04整行保持未完成。

页面维护本文件、原型片段、浏览器接口提案和共用导航／日志；后端维护内部设计与后端日志。主题最多三轮；技术ACK不代替用户对本页三项安排的同意。

后端已核r1页面目标语义及唯一修订落盘，1／3，无剩余分歧；完整收发见页面09-11日志CP10及后端CP06／CP07。核对范围只为文档，不是JS／浏览器／真实验证。用户现已采用三项UI，准确范围见页面CP11；完整F04与专用协议仍未完成。
