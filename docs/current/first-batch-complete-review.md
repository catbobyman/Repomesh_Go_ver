---
status: proposed
date: 2026-09-11
design_revision: FIRST-COMPLETE-2026-09-11-r1
updated: 2026-09-12
implementation: not-started
---

# 首批六项完整评审包

2026-09-12 B02 采用补充。用户已回复“确认，继续”，采用范围以 [B02 采用记录](b02-authentication-adoption.md)为准。本文对应认证及必要秘密子集的“待采用”描述保留原提案历史，已由该记录替代；模型、预算、运行和其他未列明部分仍待采用。实现与验收进度见 [B02 记录](../development/2026-09-12-batch-02/README.md)。

**2026-09-12 候选修订：** 审查发现的 Key 永久未知、Issue 配置归属、测试观察和登录返回缺口按[修订入口](design-readiness-revisions.md)补齐或澄清。本文 D06／P4／P7／A2／R06 随唯一专题修订，另增 P9 的 Issue 配置绑定候选；这些变化不继承旧 CP16 的技术回执或用户采用状态。下文旧检查计数仅为 09-11 交付证据，当前修订检查另记。

用户原话：“你直接给我这六项的全部”。本包一次交付六组剩余设计的具体推荐、页面原型、API字段和恢复规则，供统一评审。该句话授权完成提案，不自动采用新决定；当前仍只设计页面和API，开发另做。

**先看[六项页面总览](../prototypes/index.html#review)**。原型顶部“首批六项”可随时返回，主界面保留项目设置入口；11份原HTML保留在原型目录，4份旧稿转入历史归档，采用范围和对照入口保留。所有状态按钮都是演示，不连接真实账号、模型、数据库或执行器。

## 1. 六项交付与推荐

| 组 | 已交付的具体内容 | 原型／唯一文档 |
| --- | --- | --- |
| ① 已展示页面裁决 | Issue最小详情三项＋Key保存三项，准确范围见下表D01—D06 | [详情](../prototypes/index.html#issue-overview)、[Key保存](../prototypes/index.html#key-save)；[详情说明](issue-overview-minimal-design.md)、[Key说明](model-key-save-design.md) |
| ② 单模型测试 | 固定已保存模型与版本→可能收费确认→登记→结果；未知只查询原测试，切换页面仍可回原结果 | [测试原型](../prototypes/index.html#model-test)；本稿T01—T05及[模型字段§4](model-settings-browser-api-draft.md) |
| ③ 登录／仓库发现API | 五个新增认证端点、回调／注销竞态、独立attempt回执、期限／清理、当前覆盖及持久分页续扫 | [登录恢复](../prototypes/index.html#auth-recovery)；[认证唯一字段稿](authentication-browser-api-draft.md) |
| ④ 模型保存／测试／应用API | 共十二个端点，含既有固定版本读取和新增 Key 原保存安全终结候选；完整供应商快照与只写Key、单模型原操作、单次消费预览、精确保留execution、并发／原结果查询 | [模型](../prototypes/index.html#models)、[用于项目](../prototypes/index.html#model-apply)；[浏览器字段](model-settings-browser-api-draft.md)、[后端原子责任](backend-model-operations-draft.md) |
| ⑤ 必要配置来源 | 独立部署根与信封加密、不可变配置导入、有限请求次数账本、App逐动作要求、受控模型出站 | [只读就绪摘要](../prototypes/index.html#source-settings)；[来源S01—S08完整稿](backend-first-batch-sources-draft.md) |
| ⑥ 现有页面异常恢复 | 六包页面状态矩阵、三类原写入恢复、F02冲突对比、Issue/rooms分读、会话只读页头、迟到隔离 | [状态演示](../prototypes/index.html#recovery-states)、[项目设置](../prototypes/index.html#settings)；[恢复R01—R07](first-batch-recovery-design.md) |

## 2. ①详情与Key：六项页面决定

以下推荐均待采用，未把此前“继续”解释为同意。

| 决定 | 推荐 | 理由／代价 |
| --- | --- | --- |
| D01 | 详情依次展示标题及记录已保存、独立运行观察、目标、仓库、关联会话 | 保存事实清楚；不把刚建记录显示为执行中。 |
| D02 | 当前有权读业务会话即可打开，独立于主房间Ready；会话失权隐藏其名称和入口 | 保留可读历史入口；额外核内容范围权限。 |
| D03 | 首批详情仅基本概览、返回、会话入口和状态回读；ChangeSet只读，不放DAG/Leader/交付/执行按钮 | 收拢首批；完整过程面板后续接，旧采用的DAG/dock不撤销。 |
| D04 | Key默认保留，显式替换后先加入草稿，与连接和模型完整保存 | 避免误删旧Key；增加一次草稿确认。 |
| D05 | 保存摘要列供应商、地址、Key意图和模型数，不回显Key；不自动测试或更新项目 | 各动作边界清楚；保存后仍需明确测试／应用。 |
| D06 | 未知保留原操作，可查询、稍后查看或请求安全终结；服务端确认原保存已提交则取回原结果，确认未提交且已封闭迟到提交后才能重新编辑并新建保存 | 增加与保存争用同一唯一槽位的终结动作；404、超时和关闭弹窗均不构成终结证据。 |

## 3. ②测试：完整流程与五项决定

可点击路径：六项总览→看测试页面→勾选可能收费→确认→原型控制“调用开始／结果未知”→查询原结果→稍后查看→模型页“查看原结果”。“模拟次数额度未知”展示不能确认；“模拟运维核查关闭”只演示内部已取得停发依据后的候选结果，不是用户能解除真实未知的按钮。

| 决定 | 推荐 | 理由／代价 |
| --- | --- | --- |
| T01 固定候选 | 只测已保存的一个模型、连接和Key版本；有草稿先保存，不加“测试全部” | 能确定哪一套配置得到观察；每模型分别测试。 |
| T02 费用确认 | 预留1次请求，用户明确可能收费；首批不显示估算金额 | 不引入没有可信来源的价格数据；次数上限不能控制供应商金额。 |
| T03 最小探测 | 固定短文本，无工具/图片/流；输出min(16,模型上限)，候选30秒 | 探测连通性成本可控；不验证reasoning/vision或完整任务能力。 |
| T04 独立结果 | 登记、实际开始、通过、明确错误、未知、确定未发分开；旧结果只属于旧快照 | 避免“202=通过”；要保留原结果入口和状态时间。 |
| T05 未知收口 | 每actor最多一笔未核测试；未知不重发。运维证明原发送者失去调用能力后可审计关闭，保留unknown历史和已消费次数；之后新测试仍需重新确认；运维关闭不证明供应商已停，迟到确切证据仍可采纳 | 避免重复收费与永久无法操作；增加受控核查责任，不以租约超时冒充停发证据。 |

**金额预算替代**：若要求测试费用必须受货币金额约束，请不采用T02／S05的次数方案，改补价格版本、最大输入输出计量及未知费用预留。两者不同时冒充已采用；当前双方共同推荐次数方案以保持首批最小范围。

## 4. ③认证／发现：统一裁决范围

| 决定 | 推荐及完整范围 | 代价 |
| --- | --- | --- |
| A1 | github.com单一GitHub App用户授权；安装覆盖外仓库可能缺席，coverage如实partial，保留J1完整目标 | 首批不承诺全账号仓库发现，不加PAT或第二种应用登录。 |
| A2 | 同源HTTPS服务端会话；Origin＋CSRF；callback独立state/PKCE/浏览器绑定；Destination为站内类型白名单，覆盖已有会话，消息提交恢复类型明确属于后续发送批次 | 部署需正确域名、HTTPS和App回调；无任意跳转地址，恢复目标仍重新核权。 |
| A3 | 登录/重连各有attemptId及独立查询；当前session有效不能替代原重连回执；错误账号不合并 | 多一套短期尝试记录；断点只查询，不重用code。 |
| A4 | 新尝试使旧未完代次失效；注销只影响请求session所绑定代次；不发可能清掉新Cookie的迟到删除头 | 旧Cookie暂存但服务端拒绝；迟到旧登录Cookie可能要求再登录，不能保证响应顺序。 |
| A5 | 加密保存短期state/PKCE材料；binding绝对7天、会话绝对12小时/空闲30分钟、attempt10分钟、非秘密结果最多24小时；墓碑覆盖绑定及相关会话/尝试有效期 | 有清理及失效管理成本；期限均是本次候选，不是撤权SLA。 |
| A6 | 无cursor续同actor/连接/q的未完发现批次，完成后新首页才新批；每次上游最多2页/5秒，持久待办续扫，批次10分钟；cursor不混批 | 页面可能分次出现候选；空页和扫描失败要区分。 |
| A7 | 读取观察60秒、写重新核实距提交≤15秒；本地代次变立即失效；安装不足联系部署管理员 | 属来源稿候选观察窗口，不保证外部即时撤权；首批不加App安装管理页。 |

精确请求、联合类型、响应、错误和轮询见[认证稿§3—8](authentication-browser-api-draft.md)。发起限流5次/浏览器/分钟、10次/账号/分钟，查询2→5→10→30秒且隐藏暂停，均列入A5候选。旧GET/session及repositories字段保持原契约。

## 5. ④模型API：明确可审的契约边界

| 决定 | 推荐 | 代价／影响 |
| --- | --- | --- |
| P1 对象 | owner私有供应商；完整不可变ProviderRevision；模型行/profile稳定ID，Key替换生成SecretVersion，旧项目固定旧版本 | 更改一个参数也形成新供应商快照，新测试不继承旧版本通过。 |
| P2 输入 | 单供应商1—50模型；Key1—8192 UTF-8字节原样不trim；正文256KiB；其他长度及数值见字段稿§3 | 首批禁隐式删除/共享/默认切换，参数超限明确拒绝。 |
| P3 原子保存 | 配置、密文、模型映射、操作回执同事务；原键先核有权读取，原保存输入按后端M02比较；命中closed_without_save时直接拒绝，不比较虚构输入 | 需要秘密存储及持久操作槽位；终结按P4争用同槽位，不把Key哈希明文公开。 |
| P4 确定拒绝与保存终结 | 新模型保存/应用可查询持久rejected；保存另增closed_without_save，安全终结与原保存在同一唯一槽位串行，未提交时登记禁止迟到提交的终态，已提交时保留原结果 | closed_without_save不伪造原保存输入，不与rejected混成一种回执；不把404当失败，旧项目三写committed-only查询不变。 |
| P5 单次预览 | 测试/应用预览5分钟，绑定actor/固定快照/政策/项目修订；同预览仅一次消费，原操作重放优先 | 版本变化重新预览，不能自动续期后提交。 |
| P6 精确应用 | 专用接口只换model reference与固定版本，完整复制原execution引用/版本/政策；完全同值no-op仍保存回执 | 不能复用现行configuration PATCH；后者提交两引用并重新解析两者。 |
| P7 恢复与保留 | Key发出后清浏览器原文；保存未知可原查询或安全终结，测试/应用继续核原操作。原保存终结响应丢失仍查询或重试同一终结；保存/应用回执随业务历史期，终结占位保留规则见唯一协议；测试30天，unknown责任及不可复用墓碑持续保留 | 清理后有权410，404仍未知；无法凭旧键重建，不能删除终结占位后允许迟到保存。 |
| P8 内部责任 | 一次可能外发许可、次数原子登记、受控证据接收、白名单审计；后端M01—M08全部作为候选细化；queued10分钟只有确定未发才终结，公开结果将not_needed与open及运维关闭分开 | 崩溃可能留下unknown；不声称供应商恰好一次或费用已结算。 |
| P9 Issue配置归属 | 推荐Issue创建事务固定不可变ProjectConfigRevision引用；应用新模型只改变以后创建的Issue所用当前项目配置，原Issue和创建重放保留原绑定 | 在接收供未来执行的真实Issue/待办前落实关系；Key失效须显式阻塞，实际Manager消费和修复入口另行闭合。详见[绑定候选](issue-configuration-binding-design.md)。 |

字段唯一来源：[模型浏览器稿](model-settings-browser-api-draft.md)；内部唯一来源：[模型后端稿](backend-model-operations-draft.md)。测试15分钟新鲜度只是观察提示，不新增“近期测试通过才可保存/应用/建项”的门槛。

## 6. ⑤来源：八项内部设计一并提交

S01—S08的具体结构、默认值、导入schema和错误责任完整见[来源稿](backend-first-batch-sources-draft.md)，本包不只请求抽象“同意安全设计”。

| 决定 | 推荐及本次范围 | 代价／替代 |
| --- | --- | --- |
| S01 | 独立部署根文件＋PostgreSQL信封加密，按用途隔离；每秘密独立DEK及AAD，受控服务身份 | 部署方维护权限和独立备份；可改用KMS但增加服务依赖。 |
| S02 | 根轮换仅重包DEK，业务Key替换才新SecretVersion；紧急停用增加访问代次，保留原责任 | 必须保留解密旧备份所需根；根全丢失需重新录入/重连。 |
| S03 | 版本化部署清单整体原子导入；相同id/version异内容拒绝，默认只影响以后显式解析 | 初期部署工具维护，不新增F06完整管理页。 |
| S04 | 一种受限环境模板，默认Worker并发1；登记不表示环境Ready或执行器已接 | 首批不支持自由宿主命令或挂载；运行协议之后接。 |
| S05 | 请求次数预算：测试候选20/actor/UTC日、未核最多1；未来项目运行1000/日；分别账本。建项不预留Worker或消耗模型请求 | 不代表美元上限；unknown跨日仍保留责任，不能清零绕过；同scope/日换政策不重置，较低上限即时限制新预留，上调次日生效。 |
| S06 | 建项要求本次仓的Metadata read、Contents write、Pull requests write及当前安装；动作再核actor/分支/委派范围 | App权限不等于本地允许；禁止合并/强推/管理/workflow修改。 |
| S07 | 部署批准HTTPS基础URL、443、禁止内网/重定向/环境代理；校验后固定目标连接 | 本地模型/私网网关首批不接；可作为明确替代另审精确准入。 |
| S08 | 完整性、固定配置版本和当前可用观察分开；默认更新不暗改旧项目 | 有限/未知条件下可保存项目，不能跳过Issue必要门槛。 |

来源稿中的加密包装上限一百万次（预扣且失败不返）、导入1MiB、并发1—16、测试30秒/Worker3600秒、响应1MiB及无自动重试，均属于相应编号候选。登记运行预算不启用运行处理器；本批仍按既有创建原子性保存业务、事件及后续待办。

## 7. ⑥恢复：七项页面安排

| 决定 | 推荐 | 代价／边界 |
| --- | --- | --- |
| R01 | 加载、空、失败、失权、未知分别呈现；actor/项目/对象/查询/请求代次隔离，晚到响应丢弃 | 不能用缓存成功掩盖资格未知；原操作索引仍可保留但不授予权限。 |
| R02 | 首批六包分别有状态/动作矩阵，必要分页可继续，高级搜索暂缓 | 只保留基础浏览，完整运行/会话发送另批。 |
| R03 | 原项目创建、更新、Issue创建各自恢复页；404保持未知，410不重建；成功回执和当前资源分读 | 恢复会多一步查询；不通过关闭页面或超时判失败。 |
| R04 | F02修订冲突显示“当前值/我的修改”，明确比对后新提交；未勾改配置就不发送configuration，明确增仓不替换原范围 | 原保存未知时先查，不能进入另次保存。已采用三段顺序/开关/摘要不重审。 |
| R05 | Issue与rooms独立读取，准备中须实际观察；会话入口独立核权；只沿已有Issue SSE回读 | 房间不可用不抹掉已保存记录；无新增运行枚举。 |
| R06 | 增加一个只读会话页头GET；登录返回能表达原会话，消息内容沿已收口C/messages，后续消息提交恢复路由沿唯一消息契约 | 页头和消息均核完整内容资格；不承诺实时发送已完成，不新增第二套消息协议。 |
| R07 | 原型展示关键状态；实现后按文档故障矩阵覆盖持久化、权限和网络断点 | 本轮只走查演示，不用原型成功代替真实验收。 |

## 8. 已采用范围、剩余工作与协作

F01所问三项恢复UI、F02三段顺序/显式配置开关/摘要确认及主界面入口、F03三项选仓安排、F04三项模型应用安排继续已采用；创建弹窗/关联控件/提交反馈、模型分栏参数等早先采用范围保持。D01—D06及本包所有新增T/A/P/S/R决定仍待用户。旧DAG/dock采用不等于本批加入执行。

本次“六项”是上一条进度答复的六组剩余设计，不是最初六包页面数量，也不是F01—F15全部。开发顺序见[开发指南](../plan/DEVELOPMENT-START.md)，第一批范围的原采用依据仍见[原待做表](../archive/2026-09-12-development-preparation/docs/current/first-development-todo.md)；F06/F11—F15高级及运行面板继续暂缓。没有未授权的开发或全量运行承诺。

| 下一步 | 负责人 | 条件／完成标准 |
| --- | --- | --- |
| 统一审阅本包 | 用户；双方解释影响 | 可按编号采用/改动，明确版本与范围；未答不视为同意。 |
| 将采用范围写入唯一契约及交接 | 页面维护HTTP/UI；后端维护内部 | 保存用户原话，不覆盖旧已收口的三轮契约，不把整篇proposed一键改accepted。 |
| 实施任务与验收 | 后续获得开发授权的任务 | 页面/API/持久化/外部接入分别实现并测；当前不启动。 |

双方身份、整文件归属、完整收发和最多三轮机制见[通信约定](../archive/2026-09-12-development-preparation/docs/current/design-communication.md)、[页面CP16](../archive/2026-09-12-development-preparation/docs/current/design-communication-page-2026-09-11.md)、[后端CP16](../archive/2026-09-12-development-preparation/docs/current/design-communication-backend-2026-09-11.md)。页面ID为01a08fc7-ff7a-7590-a496-c0bd99e9e26d，后端ID为01a08fc8-7eba-7d22-81c6-08e41c107d1a，均local；不恢复旧设计师。技术核对状态按CP16最后回执，技术ACK不替代用户采用。

09-11 原交付的文档技术核对曾通过：认证r2为2／3，模型API/来源/F02/公共恢复各r1为1／3，当时回执记无遗留文档分歧，全部新增决定仍待用户。该回执不证明 09-12 修订已通过同一历史轮次，修订检查见[独立记录](../reviews/2026-09-12-design-fixes/README.md)。

已检查：5个嵌入家族生成，6个脚本语法解析通过，15份原HTML哈希一致；本包六份文档44个本地链接及10段JSON通过，已跟踪页面文档diff --check通过。浏览器实际走查总览、测试确认→登记→未知→查询仍未知→关闭/原入口重开、权限待核隐藏内容、项目修订冲突对比和配置缺项摘要，截图检查排布。原型其余分支未宣称逐一验收。

未运行产品Go/npm、OAuth、真实Key/模型、数据库故障用例或历史实验。原型全部是内存样例，刷新重置；这些检查不表示API实现、授权/密钥安全或持久恢复已验证。
