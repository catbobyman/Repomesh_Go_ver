# F01 登录与账号重连恢复页面 · RM-UI-PROJECT-ENTRY r2（三项UI已采用）

2026-09-12修订：返回目标的机器候选已在[认证浏览器草案RM-FIRST-ACCESS r3](authentication-browser-api-draft.md)补齐会话Destination、固定站内路由和重新核权；首批已有会话只读返回可表达，消息提交恢复类型留在后续发送批次。这里只修订协议衔接，F01原三项UI采用范围不扩大，认证字段与新增恢复方案仍待采用。

后端r2文档语义与一次落盘核对通过，2／3，无语义分歧；用户“是的，继续下一个”已采用所问三项UI：问题优先＋主要动作、重连未知可查询或稍后处理、确认后显式继续不自动重提业务；完整协议与其他细化仍待审。浏览器已走查过期／等待／重连未知查询和暂离再返回，仅内存样例，原文与具体边界见[页面日志CP14](../archive/2026-09-12-development-preparation/docs/current/design-communication-page-2026-09-11.md)。

用户“继续下一项”仅推进顺序，未明确采用F09三项UI或RM-FIRST-ACCESS三个方向。本次用待审候选说明登录恢复页面；不是方向已采用后的协议展开。沿已采用两步创建／授权提示和原登录视觉，原15份HTML保留；不更换布局、不实现产品。页面独占本文件和auth-recovery.prototype.js。

## 本次展示

[串联原型](../prototypes/index.html#auth-recovery)，顶部“直接查看场景”可找到“登录与重连恢复”。沿RM-UI-PROJECT-ENTRY原r1已用1／3继续r2，不用新名字重置讨论次数。r1用户“正确”仅采用两步创建及授权提示；当前三项恢复UI已按上述准确范围采用。

对应页面说明（具体参数和机器协议不随三项UI一并采用）：

1. 沿用居中登录布局，先说明当前问题，再显示一个主要动作。登录失效时只写“原页面”，隐藏其名称与内容；授权取消允许显式重试。代价是重新登录前看不到具体目标名称。
2. 账号重连与登录分开说明。当前登录仍有效时展示当前绑定账号；结果未知提供“查询本次连接结果”和“稍后处理”，不会因旧登录或旧连接有效显示本次成功。代价是需要独立结果查询协议，当前尚无采用字段。
3. 确认当前身份或本次连接后显式继续，再核目标读权；不自动重放原业务写入。首次登录尝试成功与当前身份有效不同，页面成功文案分别使用“当前登录已确认”和“本次连接已确认”。代价是多一次继续动作。

## 页面动作与API责任草案

| 页面动作／状态 | 已有依据及仍需补齐 |
| --- | --- |
| 当前登录状态查询 | 现行first-batch-browser-api-contract.md §2 GET /api/session，只确认当前稳定身份和独立GitHub连接观察。401清敏感缓存；200不证明某次登录／重连尝试成功。未知时不展示原项目名称。 |
| 发起登录／重连 | 唯一机器候选为[认证浏览器草案](authentication-browser-api-draft.md)：实际路径、严格Destination联合类型、CSRF／Origin、尝试绑定及错误。已成稿但尚未采用；本页不另定义JSON或HTTP状态码。 |
| 查询本次重连结果 | 需有权核查本次尝试与当前连接代次，不能拿现有session端点代替。原型查询维持未知，只有明确标为模拟的场景按钮展示确认结果；无真实查询。 |
| 返回工作区／继续 | 原型只演示缓存导航。协议候选用受控conversation目标恢复原只读会话，服务端按固定路由生成nextPage，继续前再次核主体、项目归属和整个会话内容读权；未知不继续，失权／跨项目／不存在统一返回工作区，不展示原标题。任意returnUrl和客户端路径旁路拒绝。 |
| 授权取消／过期／账号不符 | 文案候选；不展示供应商原始错误正文、code、state、邮箱或未经当前身份确认的账号信息。时间参数、多标签细节及完整错误映射仍待审。 |

浏览器已有字段唯一源为[首批契约](first-batch-browser-api-contract.md)；后端候选来源见[认证与配置来源草案](backend-first-development-access-draft.md)。本稿只是页面信息／动作与所需责任映射，不创建第二个/api/session定义，也不声称认证协议完成。

## 检查与未完

内存演示登录过期、用户取消、等待、当前登录未知、需重连、本次重连未知、账号不符、当前身份已确认及本次重连已确认。没有真实OAuth、存储、账号、授权或API调用；全局预览导航仍可看样例，不能作真实失权隔离验收。

登录结果查询、返回目标白名单、无权限落点、退出晚到和多标签／跨刷新等已形成唯一认证候选，仍待采用与真实实现／验收，不能沿用此前“尚无字段”的阶段描述。后续消息提交返回必须携原projectId、conversationId和submissionId，并核同一原actor；首批不开放该类型或发送入口。安装修复产品入口及尚未覆盖能力继续按首批范围延后，页面可点不表示运行就绪。当前仍只页面与API设计，开发单独进行。
