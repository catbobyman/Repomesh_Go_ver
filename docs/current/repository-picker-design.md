# F03 仓库选择器 · 页面提案 r1

用户最新指令：“继续下一项”。从F02三项已采用页面安排转入F03，不把继续视作本页采用。主题RM-UI-REPOSITORY-PICKER r1，页面主责；用户随后回复“同意，继续下一项”，采用下列三项页面安排；后端已核r1语义及唯一修订落盘，1／3，无分歧，完整收发见页面09-11日志CP09。

## 展示与待裁决范围

[串联预览](../prototypes/index.html#repository-picker)：主界面 → 项目设置 → 添加仓库，在已保存仓库下展开选择器。保留F02资料→仓库→配置的同页顺序、显式配置开关和摘要后保存；原始15份HTML不改。新增原型片段[repository-picker.prototype.js](../prototypes/repository-picker.prototype.js)由页面独占，生成器将其嵌入F02页面；片段不能单独运行。

用户已同意下列三项呈现；采用仅限这些安排，不扩展为完整发现、清理、恢复或F03整行完成：

1. 选择器内先列“本次已选”，再列搜索结果；搜索和加载更多保留选择，逐项撤回仅影响未提交增量。理由是选中项跨页仍可核对，代价是占用更多纵向空间。
2. 仓库行并列说明账号可读取与App工作能力；App待授权／待核查仍允许明确选入待配置项目。发现覆盖提示单独放在列表上方，避免混为同一种授权状态。没有伪造安装跳转，邀请用户可能需要管理员协助。
3. 完成选择回到项目表单，统一查看改动摘要再保存；未核实新增读取资格时隐藏旧名称，保留选择数量并禁止包含该增量的保存，允许明确撤回草稿选择。

## 唯一来源与边界

沿[首批浏览器契约](first-batch-browser-api-contract.md) RM-API-01 r3 §3.1—3.3、§7与[项目配置J1—J3](project-configuration-design.md)。不新增端点、改字段或开启旧r3第四轮。

- 仓库发现coverage与单仓读取资格／App工作能力分别表达。此预览一直为partial；当前结果加载完也不声称已发现全部可参与仓库。列表空搜索不是完整空集合。
- 预置5个未加入仓库，初始显示2个，加载更多与搜索仅模拟分页，选择按ID保留。本原型没有真实cursor、外部搜索或完整发现接入。
- 候选列表加载失败只演示失败提示／重试，不把仍有效的已选观察自动判为无权；若资格观察失效，使用独立的“读取资格未知”场景隐藏旧名称并阻止含该增量的保存。重试仅恢复预置样例，无真实资格保障。
- 既有项目仓库完整保留，不从返回列表重建范围。草稿撤回不会删除已保存仓库；增仓不改变在途Issue范围，不自动准备或执行。
- F02正常／未知／冲突保存路径保持。所有状态在内存中，刷新重置；当前原型对象仍持有样例数据，DOM隐藏不等于真实敏感数据清理或后端核权。

## 未完成

完整账号发现、真实授权与安装、跨页读权竞态／确认失权清理、旧请求晚到、游标失效、观察时间与具体可披露原因的完整展示、跨刷新与多操作恢复、添加集合最多100项及更新后项目总范围不超过100的校验／超限交互、创建页复用及完整异常矩阵均未完成。创建两步入口仍沿原已采用版本，本次先讨论已保存项目的添加场景。F03不标完成；F02 r1 后续已在[CP16](first-batch-complete-review.md#8-已采用范围剩余工作与协作)完成 1／3 文档核对，具体交互及业务验收仍未完成。

仅原型／文档；本页检查不作为产品实现、真实发现、授权或保存验证。

按现行§7，添加集合去重后最多100项且更新后项目总范围不超过100，超限422。候选总数不受此100项上限约束，每页limit与项目范围上限分开；本次仅修正文义，未新增额度决策或实现超限界面。
