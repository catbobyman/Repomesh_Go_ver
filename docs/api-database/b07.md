# B07 Issue 查询、最小详情与恢复

本批对应[施工计划](../plan/IMPLEMENTATION-PLAN.md)第 20 行的范围：Issue 列表、最小详情、已有会话只读、rooms 分读、SSE 与恢复页面。依赖 B06 的创建事实；本批自身不创建 Issue、不发送消息、不派工、不编辑图。

跨批通用规则中，认证主体与身份归属、操作身份与恢复边界由 [./foundations.md](./foundations.md) 拥有；统一错误外壳的字段与错误表见 [Issue 创建契约](../current/issue-page-create-api-contract.md) 第 6 节，列表分页语义见 [首批浏览器契约](../current/first-batch-browser-api-contract.md) 第 8 节，本批只引用不复述。Issue、会话、内容范围、创建操作与事件的写入权威在 [./b06.md](./b06.md)。消息与澄清的写入、投递与生命周期在 [./b09.md](./b09.md)；本批只读消费。

## 批次范围与状态

用户能完成的事：打开项目下的 Issue 列表，读取一条 Issue 的最小详情，查看当前真实的主房间与只读 Leader 房间入口，在断线或失效后按快照重读，并只读翻看已有会话的历史消息。

明确边界：不做消息发送与澄清应答、不做首次会话创建、不做 Leader 派工、不做完整 DAG 执行面板、不做图编辑、不做验证交付高级页面、不新增项目或会话列表 SSE。计划轮次 `rounds` 与 `plan_revisions` 归 B10，B11 复用同一事实，本批不定义。最小详情 UI 的采用范围仍按候选稿判断，不能因为本批存在就把整页标为已采用。

| 项目 | 当前状态 | 依据 |
| --- | --- | --- |
| 实现状态 | 未实现。没有 `/api/issues` 路由、没有 SSE 处理器、没有 `internal/issues` 包，也没有 Issue、会话、事件或房间表。 | [server.go](../../internal/web/server.go) 第 96 至 98 行只注册 auth、projects、models 三组路由；第 113 行对其余 `/api/*` 返回 404 `not_implemented`。 |
| 前端状态 | 未实现。`AppRoute` 只有首页、登录、项目、项目设置、模型设置与各类原操作恢复页。 | [routes.ts](../../web/src/routes.ts) 第 11 至 23 行、第 42 至 63 行。 |
| 已有契约采用状态 | Issue 列表、会话列表已采用设计；详情、rooms、Issue SSE 已采用设计；消息列表、单消息、澄清读取已采用设计；恢复写入路径（原键查询、清缓存）已采用设计。 | [首批浏览器契约](../current/first-batch-browser-api-contract.md) §8；[Issue 创建契约](../current/issue-page-create-api-contract.md) §7、§8；[消息澄清契约](../current/conversation-message-clarification-api-contract.md) §3、§4。 |
| 本次新增设计状态 | 本次提案，未采用、未实现：列表游标扩列、房间关联与最新观察单表、事件 `changed` 投影、前端读取代次规则。 | 本文“数据对象与表”“浏览器恢复路由与读取代次”两节。 |
| 既有候选，未采用 | 最小详情页三项 UI 安排仍待用户采用；会话只读页头是既有候选 R06。 | [最小详情](../current/issue-overview-minimal-design.md)；[页面恢复](../current/first-batch-recovery-design.md) R06。 |
| 外部运行状态 | 房间就绪、Manager 接手、真实消息投递均未接入；不得用本地占位数据伪造。 | [当前交接](../current/HANDOFF.md)。 |

## 事实基线

- 已实现的路由只有认证、项目与模型三组。[auth.go](../../internal/web/auth.go) 第 47 行注册 `GET /api/session`，第 120 行注册 `GET /api/repositories`；[projects.go](../../internal/web/projects.go) 第 33 至 140 行注册项目创建、更新、原操作查询、读取、列表、已存仓库与配置候选；[models.go](../../internal/web/models.go) 第 25 至 77 行注册六个模型端点。
- 现有列表游标已经落库，`repomesh_projects.cursors` 定义在 [0004_projects.sql](../../internal/database/migrations/0004_projects.sql) 第 149 行，kind 约束在 [0005_models.sql](../../internal/database/migrations/0005_models.sql) 第 12 至 15 行扩展到 `projects`、`repositories`、`model`、`execution`、`providers`。读写实现见 [storage.go](../../internal/projects/storage.go) 第 452 至 479 行。
- 前端已实现按主体和操作隔离的恢复存储与 401 清理。[session.ts](../../web/src/session.ts) 第 16 至 31 行在主体变化时清理恢复输入，第 47 至 48 行与第 69 行在 401 和注销时清理。
- B06 已给出创建事务的候选表名与约束，包括 `repomesh_issues.issues`、`conversations`、`issue_content_scope`、`conversation_content_scope`、`issue_event_streams`、`issue_events`、`changesets`、`project_issue_counters`。见 [B06 迁移设计](../development/2026-09-13-b04-b06-design-01/migration-design.md) 的 B06 一节。该设计状态为候选；B07 的表设计以落地时的实际迁移为准，不另建平行事实。
- 五项 B05/B06 收口固定了逐路径锁序、共同 owner account 边界和 schema2 execution 形状，产品仍未实施。见 [设计收口](../development/2026-09-14-b05-b06-design-closeout-01/README.md)。B07 的读取路径不取 owner 写锁，不重排这些锁序。

## 接口卡片

以下接口与页面恢复规则中，列表、详情、rooms、SSE、会话读取都是“已采用设计，未实现”。路径与字段保持既有契约原文，本批只补实现需要的输入输出、错误、并发和恢复细节。新增字段或新错误码逐项标注为本次提案。

### Issue 列表

调用方是 Issue 列表页 `/projects/{projectId}/issues`，也服务创建成功后的列表刷新。

| 项 | 内容 |
| --- | --- |
| 方法与路径 | `GET /api/projects/{projectId}/issues`，已采用设计。 |
| 输入 | `q` 可选，最多 200 个 Unicode 标量，对标题做字面大小写不敏感子串匹配；`repositoryId` 可选，过滤涉及该仓的事项；`cursor` 不透明；`limit` 默认 50，范围 1 至 100。 |
| 返回 | `items` 数组，元素为 `id`、`number`、`title`、`repositoryIds`、`mainChangeSetId`、`source`、`createdAt`、`revision`；`nextCursor` 为字符串或 null。正文、执行状态与隐藏对象计数都不返回。 |
| 授权 | 当前登录主体 + 项目访问资格 + 每个条目完整内容范围当前可读。先确定本地可访问对象与范围观察，再做标题过滤，防止用 `q` 探测隐藏标题。 |
| 错误 | `400 INVALID_CURSOR`、`401 AUTHENTICATION_REQUIRED`、`404 RESOURCE_NOT_FOUND`（项目或过滤仓库不可披露）、`409 CURSOR_EXPIRED`、`422 VALIDATION_FAILED`、`429 RATE_LIMITED`、`503 AUTHORIZATION_UNCONFIRMED`。 |
| 并发与重放 | 只读，无副作用，无可重放写入。分页不持有跨页数据库快照；允许 `items` 为空但 `nextCursor` 非空。 |
| 理由 | 重试和双击只产生读取；隐藏事项不进入计数、标题匹配或错误正文，避免通过搜索差异推断无权对象存在。 |

分页实现规则。服务端按 `(projectId, id)` 上已有的唯一索引做稳定 keyset 扫描，`after_id` 记录最后扫描位置而不是最后可见位置。对候选逐条核权，明确拒绝的条目跳过且不计数，内核权结果未知时整次返回 503，不得返回看似完整的成功列表。`nextCursor` 为 null 才表示遍历结束。

`repositoryId` 过滤的语义是“内容范围包含该仓”，不是当前工作范围。跨项目或当前不可读的仓库统一返回隐藏式 404；核查未知返回 503。该过滤不能与 `q` 组合成可用于探测隐藏范围的查询。

### Issue 最小详情

调用方是详情页 `/issues/{issueId}`，以及从 rooms 或会话返回后的重读。

| 项 | 内容 |
| --- | --- |
| 方法与路径 | `GET /api/issues/{issueId}`，已采用设计。 |
| 输入 | 路径参数 `issueId`，不透明稳定 ID。 |
| 返回 | `id`、`number`、`projectId`、`revision`、`title`、`description`、`repositoryIds`、`acceptanceCriteria`、`mainChangeSetId`、`source`、`createdAt`。`revision` 只表示 Issue 业务快照修订。 |
| 授权 | 需要该 Issue 完整内容范围的当前读权。未知返回 503，明确无权返回隐藏式 404。 |
| 错误 | `401 AUTHENTICATION_REQUIRED`、`404 RESOURCE_NOT_FOUND`、`429 RATE_LIMITED`、`503 AUTHORIZATION_UNCONFIRMED`，正文已清理时为本次提案的新码 `410 ISSUE_CONTENT_REMOVED`。 |
| 并发与恢复 | 读一致快照；外部核权在事务外完成；响应发出前复核内容范围版本未变，变化则重读或拒绝。 |
| 理由 | 业务记录与运行观察分开显示，刚创建的 Issue 不因缺少房间依据被标成执行中。 |

读取时序。按[首批持久化](../current/backend-first-batch-persistence.md) §4.1 的有界只读一致快照取业务记录、内容范围与本地访问依据，事务外核当前外部资格，返回前重比范围版本。页面分别读取详情与 rooms，两次请求不宣称是跨系统同一快照。

`410 ISSUE_CONTENT_REMOVED` 是本次提案的新增错误码，用于正文已被清理、身份与关系仍保留的事项。正文清理只删标题、正文、验收项与派生副本，保留范围、主会话、主 ChangeSet、配置引用与操作身份。采用本错误码时同步到拥有错误表的专题，当前错误码表见 [Issue 创建契约](../current/issue-page-create-api-contract.md) 第 6 节，本文不另立一套错误框架。

### Issue rooms 读取

调用方是详情页的房间区域与房间路由 `/issues/{issueId}/rooms/{roomId}`。

| 项 | 内容 |
| --- | --- |
| 方法与路径 | `GET /api/issues/{issueId}/rooms`，已采用设计。 |
| 输入 | 路径参数 `issueId`。不接受客户端指定 `roomId` 作为授权依据。 |
| 返回 | `issueId`；`main` 含 `conversationId`、`availability`、`reason`、`roomId`、`canEnter`、`observedAt`；`leaders` 数组元素含 `repositoryIssueId`、`repositoryId`、同形观察字段与固定 `readOnly:true`。 |
| 枚举 | `availability` 为 `ready / preparing / unavailable / unknown`；`reason` 为 `NOT_ASSOCIATED / NOT_READY / ACCESS_RESTRICTED / OBSERVATION_STALE`，正常就绪为 null。 |
| 授权 | `canEnter=true` 必须同时满足当前读权、实际关联与就绪依据。点击进入房间时重新核权，旧快照不能继续授权。 |
| 错误 | `401 AUTHENTICATION_REQUIRED`、`404 RESOURCE_NOT_FOUND`、`429 RATE_LIMITED`、`503 AUTHORIZATION_UNCONFIRMED`。 |
| 并发与恢复 | 房间观察是追加记录；读取取每条关联的最新有效观察。观察过期按 `unknown` 加 `OBSERVATION_STALE`，不把旧 `ready` 继续展示。 |
| 理由 | 会话存在、选仓列表或创建回执都不能证明房间存在或可进入；空 `leaders` 只说明当前无可披露入口，不推断没有委派。 |

观察来源是本次提案的单张逻辑表 `issue_room_links`，同时保存确定关联与最后观察状态，写入方是后台协调进程在真实取得运行观察之后。房间身份按 RepoMesh instance 加外部 room 作用域保存，裸 `room_id` 不是跨实例的全局业务身份。新观察按单调 revision 与 runtime generation 条件更新，探测失败写 `unknown` 并保留原关联，旧 generation 的迟到更新被拒绝。本批只定义读取投影与过期语义，不冻结上游房间标识的传输方式、MCP schema 或任何带凭据地址。`roomId` 是 RepoMesh 的内部稳定关联标识，不是上游地址。

### Issue SSE 失效通知

调用方是详情页的单一 Issue 级订阅，路径 `/api/issues/{issueId}/events`，媒体类型 `text/event-stream`，已采用设计。

| 项 | 内容 |
| --- | --- |
| 输入 | 路径参数 `issueId`；重连可带不透明 `Last-Event-ID`。长期令牌不放入 URL。 |
| 返回 | 连接建立后先发 `resync_required`，数据只含 `issueId`；随后发送 `event: issue.changed`，数据为 `{"issueId":"iss_103","changed":["rooms"]}`，`changed` 允许 `issue`、`rooms` 或两者。 |
| 授权 | 建立、重放和每次发送前都核当前权限；未授权连接返回普通 401 或隐藏 404，不建立成功流；失权关闭连接并停止内容。 |
| 错误与恢复 | 游标可重放时继续；游标过期、代次不符或不可验证时发送 `resync_required`，要求重查快照。默认事件保留 24 小时、每 15 秒发送注释心跳，两者是可调运行参数。 |
| 并发与重放 | 通知与其对应的本地事实变更同事务持久化，提交后发布；重复通知允许合并，不承诺 exactly-once。单快照最多一个在途查询，期间再收通知置为需再查询。 |
| 理由 | 事件只声明“某个快照失效”，权限与状态以查询为准，避免通知字段泄露隐藏房间或模型内容。 |

事件不携带模型内容、计划状态或房间名。断连不把 Issue 标为失败，也不自动重发创建请求。SSE 延后时，创建回执与原操作查询仍然独立可用。

### 项目会话列表

调用方是会话侧栏，路径 `GET /api/projects/{projectId}/conversations`，已采用设计。

输入与分页规则同 Issue 列表；返回 `items` 数组，元素为 `id`、`title`、`createdAt`、`revision`，以及 `nextCursor`。只返回会话完整内容范围当前可读的对象，不返回隐藏对象数量，未知权限整次 503。该列表不定义消息正文、发送或订阅。创建表单使用的 `GET /api/projects/{projectId}/issue-creation-conversations` 属于 B06，本批列表是项目会话侧栏的通用只读列表，两者不互相替代。

### 会话只读页头（既有候选，未采用）

既有候选，未采用；本节只登记字段与恢复要求。路径 `GET /api/projects/{projectId}/conversations/{conversationId}`，用于只读会话页的页头。

| 项 | 内容 |
| --- | --- |
| 返回 | `{"id":"conv_24","projectId":"prj_1","title":"订单讨论","revision":"convrev_1","createdAt":"2026-09-11T12:00:00Z"}`。 |
| 授权 | 需要整个会话内容范围的当前读权。未知 503，隐藏 404，未登录先 401。 |
| 错误 | `401 AUTHENTICATION_REQUIRED`、`404 RESOURCE_NOT_FOUND`、`429 RATE_LIMITED`、`503 AUTHORIZATION_UNCONFIRMED`。 |
| 不做 | 不返回 `roomReady`，不返回模型输出或状态聚合，不推断反向多主会话。 |
| 恢复 | 页头失败不显示旧标题，也不因已知 `conversationId` 授予内容；消息按消息契约独立查询。 |

依据为[页面恢复](../current/first-batch-recovery-design.md) R06。它不改变已采用的五端点，是补充的单一只读接口。

### 会话历史与澄清只读

只读消费三个已采用端点，定义在[消息澄清契约](../current/conversation-message-clarification-api-contract.md) §3 与 §4，本批不复制字段。

- `GET /api/projects/{projectId}/conversations/{conversationId}/messages?limit=50&cursor=...` 返回按提交序号升序的 `items` 与 `nextCursor`。游标绑定主体、会话、内容范围版本、水位与位置；非法 400 `INVALID_CURSOR`，过期 409 `CURSOR_EXPIRED`，范围变化 409 `CONVERSATION_CONTEXT_CHANGED`。
- `GET /api/projects/{projectId}/conversations/{conversationId}/messages/{messageId}` 返回单条消息投影，用于定位原消息、问题或答复。
- `GET /api/projects/{projectId}/conversations/{conversationId}/clarifications/{clarificationId}` 返回问题当前状态、可回复性、候选与原因码。本批只读，`canReply` 在本批页面不提供提交动作。

消息保存 `POST .../messages`、原提交查询与答复提交属于 [./b09.md](./b09.md) 的范围，本批不实现、不展示可用输入框。

只读页面必须区分“没有历史”与“历史读取未接入”。当消息存储或端点尚未实现时，页面显示“历史读取未接入，当前不能确认会话内容”，不得把 404 或空数组当空历史成功。

### 浏览器恢复路由与读取代次

已采用的路由来自[会话与独立 Issue](../current/conversation-issue-separation-design.md) RM-PAGE-01 r1：`/projects/{projectId}/issues`、`/issues/{issueId}`、`/issues/{issueId}/rooms/{roomId}`、`/projects/{projectId}/conversations/{conversationId}`。原操作恢复页 `/projects/{projectId}/issue-creations/{creationId}` 由 [./b06.md](./b06.md) 拥有。

以下读取代次规则为本次提案，未采用，建议随本批实现一并采用。

1. 每个页面上下文持有 `actor`、`projectId`、`objectId`、查询与筛选、`requestGeneration`。
2. 切换任一上下文项先增加代次并撤下旧展示，晚到响应无条件丢弃。`AbortController` 只节省请求，不被当作服务端取消。
3. 每个快照最多一个在途请求；期间收到失效通知置为需再查询，结束后补一次。
4. 401 清当前身份的敏感草稿、派生缓存、选择器与订阅；403 与 404 按错误语义清理对应受限内容；权限未知隐藏旧对象内容，不当作确定拒绝。
5. 权限恢复必须重新核查，不沿用旧代次结果。原操作是否提交只按 B06 的原操作查询判断。

## 数据对象与表

本节区分三类内容：B06 已定义的复用表、现有表的复用扩列、本次新增提案。除明确标注“已实现”外，其余都未落库。本批不新增 operation、event 或 job 通用框架；事件与创建操作唯一事实属于 B06，本批只补读取所需投影，不改 B06 的表定义。

### 复用的 B06 权威表

以下表由 B06 创建事务写入，B07 只读。字段与约束的唯一权威是 [./b06.md](./b06.md) 与 [B06 迁移设计](../development/2026-09-13-b04-b06-design-01/migration-design.md)，本文不重新定义。

| 表 | B07 读取用途 | 关键键 |
| --- | --- | --- |
| `repomesh_issues.issues` | 列表与详情的主记录，含 `project_id`、`number`、`title`、`description`、`criteria`、`revision`、`main_conversation_id`、`main_changeset_id`、`initial_configuration_revision`、`removed_at`。 | `id` 主键；`UNIQUE(project_id, number)`；`UNIQUE(project_id, id)`。 |
| `repomesh_issues.conversations` | 会话列表、页头与 rooms 主入口的业务身份；`content_scope_revision` 用于分页与读取代次比较。 | `id` 主键；`UNIQUE(project_id, id)`。 |
| `repomesh_issues.issue_content_scope` | 单 Issue 内容范围核权；列表的逐条过滤。 | `PK(object_id, repository_id)`。 |
| `repomesh_issues.conversation_content_scope` | 会话标题与历史消息的整段核权。 | `PK(object_id, repository_id)`。 |
| `repomesh_issues.issue_repository_scope` | 列表的 `repositoryId` 筛选与详情 `repositoryIds` 投影。 | `PK(issue_id, repository_id)`。 |
| `repomesh_issues.issue_event_streams` | 每 Issue 事件流计数行与代次。 | `PK(issue_id)`。 |
| `repomesh_issues.issue_events` | SSE 重放与失效投影。 | `PK(issue_id, generation, sequence)`。 |
| `repomesh_issues.changesets` | 列表与详情返回 `mainChangeSetId` 的归属校验。 | `UNIQUE(project_id, issue_id)` 限定单个主 CS。 |

列表查询需要 `issues(project_id, id)` 的稳定顺序索引，该唯一约束已覆盖。若实现把 `removed_at IS NULL` 做成部分索引，索引谓词必须与查询谓词一致，不能只在 handler 过滤。

### 列表游标：复用现有 cursors 表（本次提案）

本次提案，未采用。现有列见 [0004_projects.sql](../../internal/database/migrations/0004_projects.sql) 第 149 行，kind 约束见 [0005_models.sql](../../internal/database/migrations/0005_models.sql) 第 12 至 15 行。本次在其上做最小扩展，不新建游标表。

| 列或变更 | 类型与约束 | 人话含义 |
| --- | --- | --- |
| `kind` | 约束扩展为包含 `issues`、`conversations` | 同一张表按列表种类隔离，不能拿会话游标读 Issue 页。 |
| `filter_key` | 新增 `text NOT NULL DEFAULT ''` | 规范化过滤指纹，包含 `q`、`repositoryId` 与排序版本；参数变化时旧游标失效。 |
| `context_version` | 新增 `text NOT NULL DEFAULT ''` | 发布者定义的不透明上下文版本，用于范围或可见集合变化后要求从头刷新。 |
| `actor`、`project_id` | 已有非空列 | 游标绑定主体与项目，换主体或换项目直接不匹配。 |
| `after_id` | 已有 `text NOT NULL DEFAULT ''` | 最后扫描的稳定 ID，不是最后可见 ID。 |
| `page_limit`、`expires_at` | 已有列与约束 | 记录页大小与过期时间；过期返回 `CURSOR_EXPIRED`。 |

兼容性核对。现有主键是 `id`；读取端 [storage.go](../../internal/projects/storage.go) 第 452 至 470 行比较 actor、kind、project_id、query、page_limit，并按 `expires_at` 判定过期；写入端第 472 至 479 行显式插入九列；产品代码没有删除游标的语句，过期行由读取判定。新增 `filter_key` 与 `context_version` 并带 `''` 默认值，同时只扩展 kind 约束，不改变 B03/B04 已有行的写入与比较。采用前必须按此结论在真实数据库复核，任何清理作业单独设计，不能删除仍有效的 B03/B04 游标。

游标不是授权凭据。即使签名和存储都有效，每页仍重新核权。消息分页的游标归消息专项所有，不写进本表语义。

### 房间关联与最新观察（本次提案）

单张逻辑表 `repomesh_issues.issue_room_links` 未采用、未实现，写入方是后台协调进程，读取方是 B07。它同时保存确定关联与最后观察状态，不为只读页新增观察历史表。上游房间标识按不透明引用保存，不含凭据或地址。

| 字段 | 类型与约束 | 人话含义 |
| --- | --- | --- |
| `room_link_id` | `text PRIMARY KEY` | 本关联的内部 ID，对应响应里的 `roomId`。 |
| `project_id`、`issue_id` | 非空复合外键引用 `issues(project_id, id)` | 关联属于同一项目的同一 Issue。 |
| `kind` | 非空，取值 `main` 或 `leader` | 主房间与 Leader 房间的读法不同。 |
| `instance_id` | 非空不透明字符串 | RepoMesh 稳定运行实例身份，房间身份跨实例不共用。 |
| `upstream_room_ref` | 非空不透明字符串 | 该实例内的外部房间引用，与 `instance_id` 一起构成房间身份。 |
| `conversation_id` | `main` 分支非空，复合外键 `(project_id, issue_id, conversation_id)` 到 `issues(project_id, id, main_conversation_id)` | 主房间必须挂在该 Issue 的主会话上，只属于同项目还不够。 |
| `repository_id` | `leader` 分支非空，复合外键到 `issue_repository_scope(issue_id, repository_id)` | Leader 入口对应的仓库，必须在同一 Issue 的工作范围内。 |
| `created_at`、`retired_at` | 时间列，后者可空 | 关联退役后不再对外提供入口；退役不删除行。 |
| `availability` | 非空，取 `ready / preparing / unavailable / unknown` | 最近一次真实观察的可用状态，待办排队不映射成 `preparing`。 |
| `reason_code` | 可空，取契约枚举 | 允许披露的原因。 |
| `observed_at`、`expires_at` | 非空时间列 | 观察时刻与失效时刻；过期投影为 `unknown` 加 `OBSERVATION_STALE`。 |
| `observation_revision` | 非空 `bigint` | 单调递增的观察版本，只有更大的值才能覆盖。 |
| `runtime_generation` | 非空不透明字符串 | 产生观察的运行代次，旧代次的迟到写入被拒绝。 |
| `observer` | 非空 | 写入观察的后台执行身份，便于追溯。 |

`repositoryIssueId` 是已经采用的仓库事项业务身份，逻辑记录由[首批持久化设计](../current/backend-first-batch-persistence.md)第 1 节定义；它在同一 Issue 与仓库内全历史唯一，首次真实委派才登记。具体物理表由 [B10 的 repository_issues 提案](./b10.md)定义。本表不重复保存这个身份，也不增加指向后续批次表的前置外键。B10 持久化提案采用并实现后，Leader 投影按 `(issue_id, repository_id)` 读取该行的 id，并结合当前授权与房间关联决定可披露条目；此前没有真实仓库事项时，`leaders` 返回空数组。只选中仓库、建立计划或读取页面都不能补造事项和 Leader 入口。

分支约束写成完整 CHECK：`(kind = 'main' AND conversation_id IS NOT NULL AND repository_id IS NULL) OR (kind = 'leader' AND conversation_id IS NULL AND repository_id IS NOT NULL)`。这样一行只表达一种房间关联，不会同时挂会话和仓库。B06 的 Issue 表在 B07 采用时补 `UNIQUE(project_id, id, main_conversation_id)`，供主房间的复合外键引用。

唯一性：`main` 关联用部分唯一索引 `(issue_id) WHERE kind = 'main' AND retired_at IS NULL` 限定每个 Issue 至多一条有效主房间；房间身份用 `UNIQUE(issue_id, kind, instance_id, upstream_room_ref)` 限定，裸 `upstream_room_ref` 不能跨实例当作同一房间。另设 `UNIQUE(project_id, issue_id, conversation_id, room_link_id)` 供 B09 的 Manager 会话以完整作用域引用自己的 main 房间，以及 `UNIQUE(project_id, issue_id, repository_id, room_link_id)` 供 B10 的仓库执行引用 Leader 房间；知道同项目的另一个房间 ID，也不能把它绑到错误的 Issue 或仓库。

跨批引用：本表主键 `room_link_id` 是稳定引用目标，B09 的 `manager_sessions` 与 B10 的派工记录通过 `room_link_id` 引用本表（见 [./b09.md](./b09.md) 与 [./b10.md](./b10.md)），不复制房间可用性、原因码、观察时刻、过期时刻、观察版本或运行代次。消息与投递记录经各自会话引用间接定位房间，不保存裸 `room_id`。

本表的业务边界：它是 Issue 的 main 与 leader 房间投影，`project_id` 与 `issue_id` 非空，只为已有 Issue 登记关联。普通对话可以先于 Issue 存在（见 [./b09.md](./b09.md) 第 30 行），这类会话的运行绑定由 B09 的 `manager_sessions` 自身通过 `manager_instance_ref`、`transport_session_ref` 与 `state` 持有，不经过本表；会话关联到 Issue 后才引用本表的 `room_link_id`，并校验同项目范围。本表只承担 Issue 房间一种业务生命周期，不作为全系统统一 room 表，也不为无 Issue 会话造行。

写入规则：探测成功与新关联登记都在条件更新中按 `(observation_revision, runtime_generation)` 比较，较小或同代次的旧值不能覆盖新值；探测失败写 `unknown` 并保留原关联，不删除、不降级关联本身。读取投影只取该行的最后状态，`canEnter` 由当前读权、关联有效性与观察状态共同计算，不是存储列。观察过期时长属于运行策略，本批不冻结数值。

状态转移：关联从准入时创建为有效行，真实观察把 `availability` 在 `unknown`、`preparing`、`ready`、`unavailable` 之间按最新证据移动；探测失败只能写入更晚 revision 的 `unknown`，不能删除关联；上游关闭或重映射时置 `retired_at`，此后停止对外入口并拒绝新的观察覆盖。旧 generation 或更小 revision 的写入一律拒绝，不产生半新半旧状态。

代次分工：本表只保存运行实例代次 `runtime_generation`。Manager 逻辑会话代次 `manager_session_epoch` 归 B09 的 `manager_sessions`，两者不能互相替代；旧 Manager 代次的回写即使伴随更新的房间观察 revision 也要在那侧拒绝，本文不为它增加字段。

### 事件流与 SSE 游标

事件流与计数行由 B06 创建事务写入。B07 需要其支持通知投影与重放，因此提出以下最小增量，未采用。

1. `changed` 是按事件种类计算的读取投影，不新增列。事件唯一事实仍是 [./b06.md](./b06.md) 的 `issue_events`：当前只定义 `snapshot_invalidated` 一种事件与 `created_at`。该种类不区分失效对象，SSE 数据按保守值 `["issue","rooms"]` 投影，客户端按两类快照都要重查处理；B06 若在唯一表定义中增加能区分对象的事件种类，本投影再按种类映射为 `["issue"]`、`["rooms"]` 或两者。本文不新增列，也不建立第二个事件权威。
2. 事件插入继续在持有 `issue_event_streams` 计数行的事务内完成，序号按流递增。所有同 Issue 的事件写路径使用同一锁，禁止用全局自增序列推断提交顺序。
3. `Last-Event-ID` 表达 `(issue_id, generation, sequence)`，由服务端签名或存储保护。代次不符、序号已清理或超出保留窗口时发送 `resync_required`。默认保留 24 小时来自已采用的创建契约，B06 迁移候选把同一数值标为可调保留期，实施前以同一数值落齐。
4. 发布器只扫描已提交事件；发送成功不删除事件，也不记为浏览器已读。保留窗口清理不改变业务记录。

事件类型仍沿用 B06 的 `snapshot_invalidated`，本批不新增事件类型；`changed` 只是通知投影，SSE 线上的事件名保持已采用的 `issue.changed`。

游标过期始终走完整补查，不假装连续补齐。清理事件不级联删除 Issue 或房间关联。

### 会话与消息只读对象

会话业务身份复用 B06 的 `conversations`。消息、提交与澄清的逻辑记录属于消息专项，见[消息内部设计](../current/backend-message-clarification-design.md) §3，其字段与唯一约束不在本文复制。B07 只依赖三件事。

1. 会话内容范围核权：读取标题、消息与澄清前必须通过 `conversation_content_scope` 与当前外部观察。
2. 消息分页水位：按提交序号稳定排序，游标绑定主体、会话、内容范围版本与水位。
3. 问题快照可定位：`clarificationId` 就是问题身份，不另造 questionId。

消息写入、答复唯一性、澄清状态机与投递责任归 [./b09.md](./b09.md)，本批不实现。

### 索引与保留要求汇总

| 对象 | 必须存在的索引或约束 | 目的 |
| --- | --- | --- |
| `issues` | `UNIQUE(project_id, id)` 与 B07 增量 `UNIQUE(project_id, id, main_conversation_id)` | 列表稳定分页、详情定位与主房间的会话归属。 |
| `issue_content_scope` | `PK(issue_id, repository_id)` | 逐条核权不产生全表扫描。 |
| `conversation_content_scope` | `PK(object_id, repository_id)` | 会话整段核权。 |
| `issue_room_links` | 部分唯一索引与 `UNIQUE(issue_id, kind, instance_id, upstream_room_ref)` | 每个 Issue 至多一条有效主房间，房间身份按实例作用域唯一。 |
| `issue_room_links` 观察列 | `(issue_id, kind, retired_at)` 查询索引 | 读取最后观察，不依赖额外历史表。 |
| `issue_events` | `PK(issue_id, generation, sequence)` | 按流重放与过期判断。 |
| `cursors` | 主键与 `expires_at` 清理索引 | 游标过期与回收。 |

保留规则：事件默认保留 24 小时；游标按 `expires_at` 判定过期；房间关联行保留关联与最后观察，退役不删除、不另建观察历史；正文清理保留身份、范围与关系，详见 B06。

## 事务、并发与恢复

正常读取流程。认证与项目资格核验后，按有界只读事务读取业务记录与范围，事务外获取当前外部观察，返回前重比范围版本，然后投影响应。列表逐条核权；详情与 rooms 分开读取，各自返回自己的快照。

响应丢失。读取无写入，客户端直接重试即可。页面不得因为一次读取失败重新发送创建或消息写入请求；原创建操作只按 B06 的 `creationId` 查询。

身份边界。本批没有新的业务操作身份：创建与重放的幂等身份仍由 B06 的 `creationId` 与其作用域决定；`runtime_generation` 只是外部执行观察代次，既不能替代 `creationId`，也不能替代 B09 的 Manager 会话代次。事件载荷可以带 `schemaVersion`，但 `schemaVersion` 不参与幂等键，也不把事件变成新的业务操作。

同键异参与重放。本批接口没有幂等键。`Last-Event-ID` 篡改或跨 Issue 复用时按不可验证处理，发送 `resync_required`，不接受为授权。分页游标篡改返回 `INVALID_CURSOR`，过期返回 `CURSOR_EXPIRED`。

并发。内容范围在读取期间扩大的，按响应前版本复核结果重建或拒绝，不用旧小范围返回新正文。房间观察与读取并发时以最新已提交观察为准。事件写入与发布器并发时，发布器只看到已提交行；清理与重放并发时，先于清理开始的有效重放可完成，之后请求收到 `resync_required`。

崩溃。后台发布器重启后按主库扫描恢复，不依赖内存广播。浏览器断线后按原游标重连；不能重放时完整补查。进程重启不改变已提交的业务记录。

迟到结果。切 Issue、切会话或切项目后到达的响应按代次丢弃，不能填进新上下文。旧权限下的成功响应在失权后不构成继续读取的依据。

删除。Issue 正文清理后详情返回 410 提案码，列表不再返回该条目，SSE 关闭或要求重查。会话与消息的清理语义沿消息专项，不在本批新增删除路由。

## 生命周期

| 对象 | 创建 | 变更 | 结束 |
| --- | --- | --- | --- |
| Issue 业务快照 | B06 创建事务 | 后续业务编辑提升 `revision` | 正文清理后 `removed_at` 非空，身份与关系保留。 |
| 内容范围 | 创建时写入，只增不减 | 新内容引入时同事务追加并提升范围版本 | 不随工作范围缩减而删除。 |
| 房间关联与观察 | 后台取得真实关联后写入单行 | 每次真实观察按 revision 与 generation 条件覆盖最后状态 | 上游关闭或重映射时置 `retired_at` 并停止入口，行保留。 |
| 事件 | 与对应事实同事务插入 | 同流递增序号 | 超出保留窗口后不可重放，触发完整补查。 |
| 列表游标 | 首次请求创建 | 每页更新位置 | 过期或范围变化后失效，要求从首页刷新。 |

## 设计理由

详情与 rooms 分成两个请求，是因为业务记录已经提交，而房间是否就绪属于外部运行事实。把两者绑成一次请求，会让房间核查失败连带显示成事项读取失败，或者反过来用旧房间状态给业务记录背书。

事件只发失效通知，是因为通知无法承载权限变化后的正文。用户收到通知后重新查询，服务端在查询时重新核权，这样失权后既不会继续泄露旧内容，也不需要撤销已经发出的正确历史响应。

列表游标绑定主体、项目、过滤与位置，是因为跨页的权限和范围都在变化。绑定以后，换账号、换筛选或范围变化都会显式失效，页面从第一页刷新，避免把两段不同条件的页拼成一份看似完整的列表。

房间关联独立于选仓列表，是因为创建时选择的仓库只是工作范围。真实 Leader 房间只在首次实际委派后出现，由运行观察写入。按选仓伪造房间会让用户以为工作已经启动。

会话只读页头单独设计，是因为页头失败与消息失败的处理不同。页头拿不到时必须隐藏旧标题，而页面仍可分别报告消息读取是否可用，两者都不授予内容权限。

## 验证场景

以下为实施后的可观察预期，当前未运行。证据要求与归档结构见 [./b08.md](./b08.md)。

| 编号 | 前提与动作 | 预期结果 |
| --- | --- | --- |
| B07-V01 | 同页快速切换两个 Issue 后再读取 | 旧 Issue 的晚到响应被代次丢弃，新页不被覆盖；无旧标题残留。 |
| B07-V02 | 列表筛选包含一个已失权 Issue | 该条目不出现在 `items`，不进入总数，错误正文与 `q` 匹配结果都不泄露其标题。 |
| B07-V03 | 列表候选范围核查未知 | 整次返回 503 `AUTHORIZATION_UNCONFIRMED`，不返回部分成功列表。 |
| B07-V04 | 列表跨页期间新增更早 ID | `nextCursor` 从最后扫描位置继续，允许空页带游标；从首页刷新后能看到新条目。 |
| B07-V05 | 篡改游标、换主体使用、等待过期 | 分别为 `INVALID_CURSOR`、不匹配、`CURSOR_EXPIRED`；页面从第一页刷新。 |
| B07-V06 | 读取 Issue 正文后立即撤回一个内容仓库 | 重新读取返回隐藏式 404 或 503 待核，不返回旧正文；已发出的正确历史响应不再视为当前授权。 |
| B07-V07 | 房间无真实关联 | `availability=unavailable`、`reason=NOT_ASSOCIATED`、`canEnter=false`、`roomId=null`；不显示伪造准备中。 |
| B07-V08 | 房间观察过期后再读取 | 投影为 `unknown` 加 `OBSERVATION_STALE`，不继续显示旧 `ready`。 |
| B07-V09 | 主房间就绪后上游关闭 | 关联退役后入口消失；关联行保留可追溯，不按选仓重建入口。 |
| B07-V10 | SSE 断线、重复通知、过期游标 | 断线显示可能过期；重复通知合并；过期游标触发 `resync_required` 并要求重查。 |
| B07-V11 | SSE 连接期间失权 | 连接关闭并停止发送；不通过事件字段暗示隐藏房间存在。 |
| B07-V12 | 只读会话页头成功但消息读取未接入 | 页头正常显示；消息区域显示“历史读取未接入”，不显示空历史成功。 |
| B07-V13 | 会话内容范围扩大后继续翻旧游标 | 返回 `CONVERSATION_CONTEXT_CHANGED`，不混合旧水位与新范围。 |
| B07-V14 | 401 与换账号 | 清除对应身份的敏感草稿、派生缓存与订阅；新账号不能继承旧游标或旧响应。 |
| B07-V15 | 正文清理后读取详情、重放事件 | 详情返回 410 提案码；事件不可重放时要求重查；旧操作查询仍按 B06 返回移除占位。 |
| B07-V16 | 发布进程重启后重连订阅 | 按主库事件恢复或补齐；不因内存丢失漏发通知，也不把断连记成业务失败。 |
| B07-V17 | 旧 generation 或更小 `observation_revision` 的迟到观察到达 | 条件更新拒绝覆盖，页面继续显示较新的已提交状态。 |
| B07-V18 | 房间探测失败 | 写入 `unknown` 并保留原关联；恢复探测成功后按新 revision 覆盖，不新建第二条主房间关联。 |

## 待决事项

1. 房间关联单表的归属。本次设计选择：B07 的 `issue_room_links` 保持 Issue 房间投影，不为无 Issue 会话增加可空 `issue_id` 或多态表；coordinator 写入，B09/B10 通过 `room_link_id` 引用并复用同一实例加外部房间的身份，不新增观察历史表。普通对话的运行绑定归 B09 `manager_sessions` 自身，B09 的 `room_link_id` 需相应改为可空，会话关联 Issue 时才引用本表并校验同项目范围。这是两类业务生命周期分开，不是新增另一个 room 平台。跨批字段与引用目标见本章表定义及 B09/B10。
2. 会话只读页头。本文按既有候选 R06 收入并标注未采用。本目录按读取职责将页头放入 B07，消息写入职责仍在 B09；R06 的候选状态不因目录归类而改变。
3. 列表游标复用 `repomesh_projects.cursors` 并扩列，属本次提案，兼容性结论见“列表游标”一节。若改用签名游标而不落库，语义不变，本文的表变更降级为实现选项。
4. `410 ISSUE_CONTENT_REMOVED` 是本次提案的新错误码，待拥有统一错误表的专题登记或改名，当前见 [Issue 创建契约](../current/issue-page-create-api-contract.md) 第 6 节。
5. 房间观察过期时长、事件保留长度以外的运行参数不采用候选数值，待运行策略专题确定。
