> 历史归档（2026-09-08）：仅供追溯，原文中的状态和待决定项不作为现行规则。当前设计见[文档入口](../../../current/README.md)。原位置：docs/HANDOFF-2026-09-07-v2.md。

# RepoMesh 产品重构交接（2026-09-07 / v2）

> 后续设计请先阅读 [v3 handoff](HANDOFF-2026-09-07-v3.md) 和[下一会话 prompt](NEXT-SESSION-PROMPT-2026-09-07.md)。本文件保留为历史交接。

更新时间：2026-09-07。当前阶段：产品与架构设计，未实施部署。

本文接替 [归档 handoff](../../HANDOFF-2026-09-07.md)，整理用户已确认的决定与未决问题，不通过交接文字自行新增产品决定。优先依据用户最新确认、当前 ADR 和术语表；旧文用于追溯讨论。本次续更纳入已确认的 ADR-0003。

## 1. 接手入口与讨论位置

实际工作区：`D:\Project4work\Repomesh_Go_ver`，Windows / PowerShell。最初交接检查未见应用源码或 .git；当前继续按设计文档工作区处理，不能从目录名称中的 Go 推断技术栈已完整确定。

请先完整阅读：

| 文件 | 内容 |
| --- | --- |
| [CONTEXT.md](../CONTEXT.md) | 项目、项目草稿、项目仓库范围、issue、角色、会话、Task、Attempt、候选及交付组合的领域定义。 |
| [ADR-0001](adr/0001-agentteams-issue-concurrency-and-isolation.md) | D01–D18：团队组织、issue 路由与会话、单机隔离、多项目独立实例、首个 issue 延迟准备；含 5 张字符图。 |
| [ADR-0002](adr/0002-github-app-authorization-and-draft-pr-delivery.md) | 仓库范围、GitHub App 分层授权、发起人权限上限与 draft PR 交付边界。 |
| [ADR-0003](adr/0003-plan-change-authorization-and-activation.md) | 计划变更的六条规则及首期自然语言调整入口；直接编辑编排图列为后续待办。 |

当前讨论主线仍为**测试、合并和回退**。其中“计划变更如何获准和生效”已确认并写入 ADR-0003；具体测试门槛、PR 时机、合并和回退流程仍待讨论。不要重新回到“是否逐仓勾选只读/推送/合并”。

## 2. 已确认的组织与隔离基线

- 使用 AgentScope 团队的 agentscope-ai/AgentTeams，不从零重新选型。
- 一个 RepoMesh 管理多个长期、多仓库项目，每项目对应独立 AgentTeams 实例，实例内部一个长期 Manager。
- 项目内每仓一支长期 Team，包含 Leader 和 Workers；不同 issue 复用角色身份，不重建 Manager 或团队。
- 初期 Controller、Matrix、存储和网关也按实例分开，接受重复基础开销。房间、具名 Manager 或目录前缀不能替代项目隔离。
- 当前最多一台服务器，RepoMesh 统一管理全局容量与项目额度，不能让每个实例按独占整机调度。
- 一个 Worker 同时只有一个活跃任务，领取和占用由后台原子保证。每次 Attempt 独立 clone、分支、构建目录及结果记录；worktree 不是安全边界。
- 普通容器不承诺 VM 或恶意多租户级隔离；Agent 不直接持有宿主 Docker socket，可信管理面的创建参数也要受限。
- 跨项目接入同一远端仓库时，本地隔离不隔离远端 Git 历史，仍需协调任务分支、基线变化与交付。
- 停止进程、回收副本和删除历史证据是不同动作；回收前保存必要状态，关键数据需要服务器外备份。

## 3. 项目创建与首个 issue：D18

用户已确认：先保存项目草稿，完成仓库授权和必要配置；第一条 issue 正式提交时才 setup AgentTeams。

```text
Save Project
    |
    v
Draft: required configuration incomplete
    |
    v
Configured: instance not created
    |
    | Submit first Issue
    v
RepoMesh persists Issue -> Waiting for project setup
    |
    v
Provision instance + Manager + Teams
    |
    +-- Failure -> Keep Issue and evidence -> Retry
    |
    v
Readiness checks pass
    |
    v
Create Issue rooms -> Trigger sessions -> Route and plan
    |
    +-- Later Issues -> Reuse this instance
    +-- Instance stopped -> Restore existing instance and data
```

- 打开页面、输入未提交草稿、保存项目或完成仓库授权，都不触发实例创建。
- 草稿表示必要配置未完成；“已配置但实例未创建”是另一个状态。完整页面文案和状态枚举尚未全部定稿。
- 先登记、核权、去重并保存 issue，再准备实例；失败时需求保留，重复事件和重试不产生第二套实例。
- 准备流程遵守全局容量约束；容量等待、失败恢复及部分组件就绪的页面还需设计。
- 之前曾建议“点击准备实例”作为另一入口；它未成为当前首次启动基线，不要悄悄恢复提前启动入口。
- 已决定以首个 issue 延迟启动，尚未完整确定项目配置完成条件。不要将这两件事混为“创建流程全部定稿”。

## 4. Issue、房间与 session：D17

跨仓或影响范围未知的 issue 默认由 Manager 主责。明确限定单仓且未发现跨仓依赖时可直达 Leader；来源仓库不能单独证明影响范围。单仓工作发现越界时升级给 Manager，保留原 issue 历史。

```text
Cross-repo Issue I101
|
+-- Main room R-I101
|     +-- Manager -> its own session
|
+-- Repo room R-I101-A
|     +-- Leader A -> its own session
|     +-- Worker A1 -> its own session -> Task / Attempt clone
|
+-- Repo room R-I101-B
      +-- Leader B -> its own session
      +-- Worker B1 -> its own session -> Task / Attempt clone

Single-repo Issue I102
|
+-- Primary repo room R-I102-A
      +-- Leader A -> its own session
      +-- Assigned Worker -> its own session
```

- RepoMesh 受控建房、邀请、设权，并维护 project / issue / repository / agent / room / session 映射。Manager 或 Leader 可以提出建房需求。
- Worker 默认加入仓库 issue 房间，普通 Task/Attempt 不逐个建房；特殊权限、超大上下文或专项验证等明确需求才考虑专用房间。
- 按此前研究的 QwenPaw 路径，建房、邀请和加入不等于创建模型 session；Agent 首次处理有效消息时按 matrix:{room_id} 查找或创建自己的本地 session。
- 同房不同 Agent 各自维护 session。共享的是可见消息，不是共同模型内存、隐藏推理或未发布工具结果；相同 session 字符串不能作为跨 Agent 的全局唯一标识。
- 私聊同样按私聊房间划分；长期 DM 不会隔离多个 issue。@ 选择/触发接收者，同房再次提及延续原会话。当前讨论的普通 reply/thread 不构成独立 session 边界。
- Manager 若也参与仓库子房间，会有该子房间自己的 session；不能假设它自动继承主房间完整上下文。具体消息桥接和结果回流需验证。
- Team Room 保留公共协作信息，其他 issue 的临时指令、审批和未完成计划不能当作共享长期记忆。
- Manager / Leader 采用短轮事件驱动协调，Worker 按容量并行。多个 issue 同时在途不等于承诺 Manager 同时多路模型推理。
- Worker 单活跃任务约束跨房间生效，不能因有两个 session 就同时接两项执行任务。
- Task/Attempt 通知和迟到结果要明确归属；独立 clone 不会自动清除同 issue session 的旧指令，重试上下文选择仍需验证。

这里的产品拓扑已确认，具体 session 时机、消息过滤、队列、上下文和重启恢复仍未做端到端验收。

## 5. 仓库范围与权限：ADR-0002

用户确认的意图：仓可以多给，权限分层收窄；Agent 改代码和开 PR，合并由人和仓库规则决定。

| 层级 | 已确认边界 |
| --- | --- |
| 选仓 | 添加仓库选择全部或指定范围，不设置逐仓只读、推送、开 PR、合并的操作菜单；App 安装范围、项目范围和任务范围分开。 |
| App | 使用固定工作权限集，目标覆盖读取代码、建/推任务分支、创建 PR、读取检查状态等主路径。 |
| 运行时 | 凭证或受控工具能力进一步收窄到相关项目、仓库、任务；不默认启用 GitHub Issues 操作、重跑 CI 或仓库管理。 |
| 发起人 | 不通过 Agent 放大用户的有效 Git 权限；App 能访问不代表当前用户有权访问，RepoMesh 要服务端核验。 |
| 交付 | 默认推自己的任务分支并开 draft PR，合并由人和仓库规则控制。读取规则不等于有权修改、绕过规则或直推主分支。 |

- “先只读接入、以后逐项启用写权限”的建议未被接受。
- 多仓共同修改仍由各仓 Team 分工，未授权单 Worker 获得所有仓库的可写共享工作区。
- GitHub App 权限名称、token 实际收窄维度、用户权限约束和分支规则强制效果尚未技术验证。不能把“产品禁止 Agent 合并”写成“已证明 GitHub 有能开 PR 但无法合并的 token”。
- RepoMesh 内部 issue 不等同于 GitHub Issues API 对象。外部 issue 同步的身份与权限另行明确。
- “全部仓库”是否动态包含后续新增仓库、授权撤销及部分失败如何呈现、项目配置完成条件尚待细化。
- 本决定曾暂记为 D19 / ADR-0001 第 6.4 节，已拆为独立 ADR-0002。现行 ADR-0001 保留 D01–D18；授权决定引用 ADR-0002。

## 6. 计划变更决定与后续讨论

### 已确认：计划变更如何获准和生效

[ADR-0003](adr/0003-plan-change-authorization-and-activation.md) 记录用户明确确认的六条规则：

1. 审批对象是明确的计划版本，展示差异、原因、依赖、仓库范围、预算和循环上限；后续修改产生新版本。
2. 全自动（YOLO）按既有授权、预算和策略自动判断，审批档由人判断，两档共用执行规则。扩大权限、提高预算上限、放宽已确认验收条件都由人决定，合并仍由人和仓库规则控制。
3. 已批准范围和上限内的 Loop 可自动运行；新候选仍需重判固定组合的证据有效性，越出循环范围或上限形成新请求。
4. 等待审批时旧计划仍有效，不能继续的工作明确阻塞。变更获准后才进入应用流程；第一版按受影响上游 Project 暂停新派工，等待在途任务和已提交结果收敛，核对、应用并读回成功后才生效。
5. 拒绝、冲突、失败和不确定结果保留历史；冲突后读取最新状态重新判断，不确定时先核查。
6. RepoMesh 保存许可并实际控制变更应用和任务启动；Controller、MCP、存储写入路径如何受控仍需验证，不能只做页面审批。

计划变更页面分别显示待审批、已获准待应用、应用中、已生效、已拒绝、需重新判断和应用失败。这些状态不替代 issue、Worker 或 Attempt 状态。单分支暂停暂不作为首期承诺；首次计划审批入口、审批人、档位默认值及切换细节尚未敲定。

已确认首期编排调整仅支持自然语言：用户提出要求，由 Manager 形成版本化变更提案，继续按上述许可与生效流程处理。直接编辑节点、属性、依赖连线及拖拽编排进入后续待办设计，不属于首期范围。

### 尚待讨论：测试、验证与交付

以下是建议顺序，不是新决定或实施指令。使用一个具体场景：商城项目含 frontend 和 backend，一条跨仓 issue 同时改变 API 和前端调用，两支 Team 已产生候选提交。

第一问可从这里开始：**两个仓库有了候选提交后，什么时候在 RepoMesh 展示为“可供人工审查”，需要哪些验证证据？**

后续逐个讨论：

1. Worker 自测、Leader 初审、独立验证的职责，页面如何显示未验证、失败、阻塞和可审查。何时创建或更新 draft PR 尚未定。
2. 固定哪个 commit 组合进行跨仓测试、在哪里运行、谁管理临时服务和数据、怎样关联 GitHub CI。
3. 人工分别合并多仓 PR 时，如何展示依赖、顺序和基线变化；一个已合并、另一个失败如何处理。
4. 回退是关闭未合并 PR、撤销已合并代码、恢复部署版本还是恢复数据；应分别定义动作、职责和授权。

保留已确认底线：候选提交不等于 issue 完成；跨仓验证绑定固定候选版本组合；组合变化要重新判断证据有效性；专项验证环境不直接向业务仓推送修复；合并由人和仓库规则决定。具体测试门槛、PR 时机、人工合并顺序、部分合并失败及回退流程尚未定稿。

需要历史材料时再读：

- [人工监管与审核](../../legacy-product-design/06-人工监管与审核.md)
- [验证与交付](../../legacy-product-design/07-验证与交付.md)
- [异常恢复与回滚](../../legacy-product-design/08-异常恢复与回滚.md)
- [需求规格与计划](../../legacy-product-design/04-需求规格与计划.md)
- [Cloud Agent 平台 PRD](../../reference/Cloud-Agent平台产品PRD.md)

旧文仅为参考，不能自动把全部旧流程搬进新 MVP。ADR-0003 只确认计划变更规则，不代表已经确定跨仓验证小组的常设组织、完整 Graph 模板、正式审查门槛或 PR 时机。后续独立且经用户确认的架构取舍按顺序另建 ADR。

## 7. 上游证据与技术待验证项

此前 ADR-0001 的 AgentTeams 源码核查快照为 eeaab64391ccaec9118e84977f538aefd40720d6（文档记录提交日期 2026-09-05）。本次 handoff 整理未重新联网核查此提交。

后续已完整阅读 [调研汇总](../../../agentteams-survey-2026-09-07/agentteams-survey.md)、[接口调查](../../../agentteams-survey-2026-09-07/agentteams-api-cli-survey-2026-09-07.md) 及其 HTML 展示版。它们是锁定提交的静态调查记录，不能替代运行验收。关键约束：TeamHarness 有 DAG/Loop 工具；Controller `replan` 在存在 `in-progress/submitted` 任务时拒绝；`pause` 只暂停新调度；Manager 全权限及部分 MCP 提示级约束尚不能强制实现 RepoMesh 审批档位。报告中 Dashboard 较新 HITL 能力的兼容性仍未验证。

调研汇总第 12 节及其 HTML 仍含先准备实例、后接收 issue 的旧流程，按 D18 解释和使用：首条 issue 正式提交、持久化后才准备实例。调研中的执行映射和适配器建议不自动成为已确认决定。

历史研究发现具名 Manager 不等于独立管理身份；共享 ServiceAccount、固定 Matrix / Gateway 身份、公共目录与端口、Team 归属问题见 ADR-0001 第 8 节。QwenPaw 按房间构造 session、Manager 原生 Project Room 和计划元数据可作为基础，尚需 RepoMesh 适配。业务 Project 不直接等同上游同名对象。

会话期间曾通过公开 QwenPaw main 页面补充 session 和历史缓冲说明；main 不等于已锁定的 AgentTeams 运行依赖，不能直接推广到 OpenClaw 等其他 runtime。最终 runtime、release 和依赖版本仍需固定后验证。

主要待验证项：

- 新 issue 房间与原生 Team Room 派工、权限、消息触发和回报能否完整兼容。
- 多 session 的实际并发、消息缓冲、长期记忆隔离、重启恢复、暂停和迟到结果归属。
- 全局调度如何强制约束各实例，防止绕过额度启动执行。
- 多实例名称、网络、端口、存储、工作目录与凭证隔离；高权限控制 API 的参数限制。
- 服务器规格、实例常驻开销与构建/集成资源峰值；不能承诺固定项目数和 Worker 数。
- GitHub App 的固定权限集、运行时收窄、个人权限上限、分支限制和授权撤销。
- 跨项目共享远端仓库的分支归属及验证证据失效处理。
- 计划变更的许可强制执行、上游可重规划条件、冲突与不确定结果核查、REST/MCP 并发写入和 Loop 接入；跨仓 issue 到上游 Project 的映射及暂停范围。

## 8. 本轮完成情况与工作约定

本会话已完成：记录 D17 房间拓扑、D18 首个 issue 延迟准备，维护术语表，将授权边界独立为 ADR-0002，整理新 handoff 并归档旧版。Markdown 结构和本地引用可检查，不代表运行方案验收通过。

后续续更已完成：阅读 Cursor Dashboard 本地报告及 AgentTeams 接口调查；用户明确确认计划变更的六条规则，写入 ADR-0003，并同步本交接及术语定义。此前讨论的测试门槛、PR 时机、验证团队配置仍待敲定。用户另已确认首期仅支持自然语言调整编排，直接编辑进入待办设计，已同步 ADR-0003。

未完成：应用实现、AgentTeams 安装部署、真实实例创建、多 issue 并发/权限隔离验证、真实仓库修改/PR/合并，以及完整新 PRD、页面原型与最终 MVP 清单。

用户偏好和授权边界：

- 中文讨论，从实际操作场景解释，少堆术语与状态码，每次一个关键设计问题。
- 先简述现状，再继续讨论；不重复追问已确认的选型和组织模型。
- 区分已确认、待建议和待技术验证。新决定经用户确认后写 Markdown。
- 术语表只放概念定义，独立主题按需使用独立 ADR，图用字符图。
- 当前只做设计和本地文档整理；暂不安装、部署、编码、创建真实 GitHub App 或写远端仓库。
- 历史子 Agent 只读研究不代表当前授权运行团队或自动创建新的 Codex 任务。

## 9. 新会话 Prompt

复制以下内容到新会话：

```text
请接手 RepoMesh 产品与架构设计，先完整阅读：

1. D:\Project4work\Repomesh_Go_ver\docs\HANDOFF-2026-09-07-v2.md
2. D:\Project4work\Repomesh_Go_ver\CONTEXT.md
3. D:\Project4work\Repomesh_Go_ver\docs\adr\0001-agentteams-issue-concurrency-and-isolation.md
4. D:\Project4work\Repomesh_Go_ver\docs\adr\0002-github-app-authorization-and-draft-pr-delivery.md
5. D:\Project4work\Repomesh_Go_ver\docs\adr\0003-plan-change-authorization-and-activation.md

旧 handoff 已归档到 docs\archive\HANDOFF-2026-09-07.md。以新 handoff 和当前 ADR 为准，需要背景时再读 docs\old_ver 和 docs\Cloud-Agent平台产品PRD.md。

已确认：使用 agentscope-ai/AgentTeams；每多仓项目一个独立实例、一个 Manager，每仓一个长期 Team。项目先保存草稿并完成配置，第一条 issue 正式提交后才准备实例，后续 issue 复用实例。Manager 主责的 issue 使用主房间和每仓子房间，明确单仓可直达 Leader；Worker 默认加入仓库 issue 房间，各 Agent 有自己的 session，普通 Task/Attempt 不逐个建房。一个 Worker 同时只执行一个任务，每次 Attempt 独立副本，全局容量和项目额度共同约束执行。

仓库接入只选范围，不设置逐仓操作权限菜单。GitHub App 使用固定工作权限集，运行时再收窄，不能放大发起人的有效 Git 权限。Agent 负责改代码、推自己的分支、开 draft PR；合并由人和仓库规则决定。ADR-0001 保留 D01–D18，历史 D19 已迁入独立 ADR-0002。

ADR-0003 已确认计划变更的六条规则：明确版本；YOLO 与审批档共用执行约束；已批准 Loop 在范围和上限内自动运行；获准与生效分开，等待上游可重规划后应用并读回；失败与冲突保留历史；RepoMesh 实际控制许可、变更应用和任务启动。不要重复追问这些决定，也不要把它们表述成已实现的上游审批能力。

已确认首期编排调整仅支持自然语言：用户提出要求，由 Manager 形成版本化变更提案，继续按上述许可与生效流程处理。直接编辑节点、属性、依赖连线及拖拽编排进入后续待办设计，不属于首期范围。

接口依据见 docs\agentteams-survey-2026-09-07。报告中的 replan 限制、pause 语义、MCP 权限和版本边界需要遵守；它是静态调查，未部署验收。其中旧的提前准备实例流程按 ADR-0001 D18 纠正理解。

下一轮按我指定的方向讨论“测试、合并、回退”。先简要说明理解的现状，然后从 frontend + backend 跨仓 issue 的实际场景开始，每次只聚焦一个关键问题，讲清页面、系统行为、状态、职责与异常处理。可先讨论：两个仓库已有候选提交，什么时候在 RepoMesh 展示为“可供人工审查”，需要哪些验证证据？这是讨论入口，不是已确认的新门槛。

保留固定 commit 组合验证、候选变化重新判断证据有效性、验证环境不直接推送业务修复等已确认原则。具体测试阶段、PR 创建时机、人工合并顺序、部分合并失败及回退办法仍需逐项讨论。

不要从零重新选型或重复追问已确认决策。区分已确认决定、待讨论建议和待技术验证项；不要把上游部分基础能力、源码阅读或 ADR accepted 表述为完整方案已经实现或部署通过。新决定经我确认后写入 Markdown；独立主题按需新建 ADR，术语表只放定义，图用字符图。
当前只做设计和本地文档整理，暂不安装、部署、编码、创建真实 GitHub App 或进行远端仓库写入，也不要自动新建 Codex 任务。
```
