# 首批页面／API设计待做表 · 范围已确认

**2026-09-12 审查修订：** [多 agent 审查](../../../../reviews/2026-09-12-design-readiness/README.md)发现 Key 保存恢复、Issue 配置归属、测试观察及登录返回的缺口，修订和分阶段开工条件见[修订入口](../../../../current/design-readiness-revisions.md)。新终结协议、绑定时点及导航扩展仍为候选；原型一致性和 Skill 适用说明按既有语义修正。业务仍是骨架，真实执行的[五项协议门槛](../../../../current/execution-integration-gates.md)未全部完成。下方 CP16 和“当前／无剩余分歧”描述保留为此前记录，不代表本轮新修订已采用或系统已通过验收。

**当前：首批六项完整待审包已成稿。** 用户“你直接给我这六项的全部”授权一次集中交付，见[总包](../../../../current/first-batch-complete-review.md)及[六项原型入口](../../../../prototypes/index.html#review)。已补认证/发现、模型保存/测试/专用应用、来源S01—S08及页面恢复；全部新增决定待用户，不逐页停问。F01/F02/F03/F04已采用的具体UI保留，F09及Key三项未采用；旧已收口r3不重开。只页面和API设计，开发另做。认证r2为2／3，模型API/配置来源/F02/公共恢复各r1为1／3；双方本次文档及定点核对均通过，无剩余文档分歧，无技术待回复。所有新增D/T/A/P/S/R仍待用户统一采用。最新收发、轮次及TODO见页面日志CP16；下方按历史时间记录的“未成稿/技术0轮”等状态以本条和CP16为准。

用户“是的，继续下一个”已采用F01登录恢复所问三项UI，技术r2、2／3；随后“继续”推进当前[F05 API Key保存与原结果查询](../../../../current/model-key-save-design.md)，r1文档核对通过、1／3，三项新UI仍待用户。F09和认证三方向未因此采用，完整Key／测试／应用协议仍未完成。只页面／API设计，开发另做，准确收发与TODO见页面CP15。

用户“继续下一项”后，当前展示[F01登录与重连恢复r2](../../../../current/login-recovery-page-design.md)。沿原入口主题第二轮，三项新UI待审；F09三项及认证A／B／C方向未因“继续”自动采用。当前只页面／API设计，开发另做；最新原文／轮次／待回复与TODO见页面日志CP14。

用户最新原话：“同意，现在收拢范围，只做最关键基本的的几个页面，我要开始开发了。现在先给我一个待做表”。

“同意”对应上一轮F04三项页面安排：核对替换前后模型／固定快照／保留execution版本，参数详情折叠；预览缺失或变化暂停确认并重新读取；未知保留原操作且只查询该次结果。本次仅记录采用和提出开发待做表，没有编码或新增业务验证授权。用户随后回复“开始按这个继续”，采用此表范围与推进顺序；又明确“现在还是设计页面和api,开发之后单独做”。当前只推进页面／API设计，开发另行开展，未定页面和协议仍逐项用户确认。

## 建议首批范围

先完成“登录 → 建项目 → 创建Issue → 查看Issue → 修改项目资料”的真实管理闭环。主界面保留会话入口与列表，不先做全部运行面板。最小模型配置属于正式创建Issue的前置依赖之一；缺模型／App必要条件时必须阻止新建，不能为缩范围跳过核验。管理闭环完成不等于Manager工作闭环完成。

| 顺序 | 页面／交互 | 首批最小范围 | 剩余设计／开工条件 | 页面采用与协议状态 |
| --- | --- | --- | --- | --- |
| 1 | 登录与创建项目 | 登录／退出、选仓、项目名称和用途、保存后进入项目；原两步顺序保留。 | 补最小登录回调、拒绝／过期／返回路径和账号读取资格；候选用真实发现覆盖提示，不要求先做完发现增强。明确实际候选来源及权限检查，不拿模拟登录开工。 | 两步入口／授权提示、F03三项已采用；认证与完整发现协议有缺口。F01／F03。 |
| 2 | 主工作区 | 项目切换、会话／Issue入口、基本列表、当前项目上下文；项目设置在主界面可找到。 | 补加载／空／错误／失权、必要分页和深链接；会话记录能读不代表能实时聊天。先不做高级筛选或全部右栏。 | 会话／Issue分离、dock布局及设置入口要求已采用；基础列表契约已有，完整导航状态未收口。F07。 |
| 3 | 创建Issue弹窗 | 标题、目标、明确仓库范围；默认新会话／可选已有会话；提交反馈与原结果查询。 | 先按创建契约核验本次范围与必要模型／App配置（canSubmit不代表任意组合可提交）；再补重载后的原操作恢复、同键冲突、404仍未知／410不重执行及失权清理；已有会话候选失效要拦截。 | 弹窗／关联控件／反馈已采用，创建v1契约已有。F08。 |
| 4 | Issue详情概览 | 标题、目标、仓库范围、业务状态、关联会话入口；准备未完成／失败如实显示。 | **下一张需要优先展示裁决的页面**：只核这几个基本信息和入口，不扩到DAG、交付详情或执行按钮。实际状态字段按已有契约与真实能力显示。 | 原概览展示过但未获明确UI采用；基础详情契约已有，运行状态协议尚有缺口。F09。 |
| 5 | 项目设置 | 修改名称／用途、明确增仓；显示当前配置来源，保留旧范围；摘要后保存。 | 对F02做一次现行契约呈现核对（此前技术仍0／3）；落实修订冲突／未知恢复。首批可先只读配置摘要，显式改引用入口按已采用F02和既有契约另行接入，不伪装精确只换模型。 | F02／F03各三项已采用；项目PATCH与查询契约已有。F02／F03。 |
| 6 | 模型设置（Issue创建的配置前置） | 沿已采用供应商分栏，最小供应商连接／单个可用模型、只写Key保存、逐模型测试、用于项目。先不新增禁用／删除／共享／默认切换等管理能力。 | **开发阻塞项**：Key保存与原操作恢复、测试权限／费用／结果查询、模型专用应用的固定快照比较／原子保留execution与恢复协议尚未完成。先补最小契约，不拿现行完整PATCH替代。 | 分栏／参数填写、F04三项页面安排已采用；真实Key／测试／专用应用协议未完成。F04／F05。 |

这里按6个页面／交互交付包组织，不是宣称只有6个路由；创建Issue是弹窗。保留所有旧采用和F01—F15记录，只改变建议排期，不把暂缓项删除或标完成。

## 分批交付建议

第一批前置仍需明确真实有效的模型／执行配置引用、固定版本、密钥和当前能力观察来自哪里，并核验适用额度限制与本次仓库范围的App能力。已有项目可受限保存，不等于Issue可创建。采用何种配置接入范围须用户确认；本表不新增默认配置、临时写Key入口或放宽创建门槛。

- 第一批：上述6项的最小范围，完成真实项目与Issue管理闭环。已有可用且被实际核实的配置时可先开发Issue页面；若没有，先补最小模型配置链路，不把模拟默认配置或跳过必要条件当作闭环完成。可先按已收口创建／列表／项目更新契约拆开发任务；尚缺认证方案、详情UI或行为决定的部分先补齐，不据原型自行冻结。
- 第二批：主工作区的真实会话发送／答复和最小准备状态。模型配置、首次消息、后台准备与真实投递均闭合后，才能说“用户发消息后Manager能开始工作”。现有消息五端点不覆盖首次会话全链路。
- 每页保留与实际写入有关的权限、错误、未知和原操作恢复；收拢功能不削掉这些基础条件。具体超出已采用范围的页面安排仍先展示用户决定。

## 跨页面后端基础（不增加页面）

| 待做 | 首批最低交付 |
| --- | --- |
| 主体与当前授权 | 真实登录主体、项目资格、仓库读取及必要App能力观察；不能用原型账号或旧授权快照代替。 |
| PostgreSQL持久化与迁移 | 按已采用的持久化设计落库，保留关系、内容范围、修订和固定配置；不是只做前端内存列表。 |
| 原子创建与后续责任 | 必要Conversation、Issue、主ChangeSet、来源／内容范围、输入快照、幂等结果、持久事件及后续待办按既有契约同事务保存。不得只保存Issue行而遗漏关联与后续责任。 |
| 三类写入的原结果恢复 | 项目创建、项目更新、Issue创建分别保留原操作作用域和结果；覆盖重复提交、未知查询、当前失权以及必要故障验收。模型写入恢复另依尚缺专用契约。 |
| 未接入后台的真实状态 | 准备／消息处理器可后续接入，但第一批仍持久保存后续待办，按已有INTEGRATION_NOT_AVAILABLE等边界表达未接入；不假写preparing／ready，不漏存待办或取消既有异步接续。 |

以上依据[创建契约§4](../../../../current/issue-page-create-api-contract.md)、[首批持久化§2.2／§3](../../../../current/backend-first-batch-persistence.md)。后端负责实现前设计完整性与后端任务拆分；当前按表继续页面／API设计，不启动开发，也不预铺新队列框架或完整运行协议。已有设计不等于这些基础已实现。

## 暂缓设计与开发的建议

高级列表筛选／全站搜索、完整仓库发现增强与批量管理、多模型高级管理、独立执行配置编辑（F06）、多Issue复杂消息与更正（F11）、完整Leader／实例生命周期面板（F12）、动态DAG及取消重试等执行操作（F13）、验证交付与证据面板（F14）、仓库分析（F15）。已采用只读DAG／dock样式保留，暂缓不撤销采用。

## 当前最值得补的三个交付物

| 负责人 | 待做 | 完成标准 |
| --- | --- | --- |
| 页面设计师2 | 精简Issue详情概览原型 | 展示基本信息与会话入口，用户明确采用具体范围。 |
| 后端设计师2主责，页面维护浏览器字段 | 最小登录／账号发现接入契约 | 真实主体、回调／退出／拒绝／过期、仓库候选来源与授权观察可供开发；新增决策获用户同意。 |
| 后端设计师2主责，页面维护浏览器字段 | 最小Key／测试／模型专用应用契约 | 固定快照、当前权限、原子比较、密钥只写、回执与未知查询可供开发；新增决策获用户同意。 |

依[页面交接](HANDOFF-PAGE-API-DESIGN.md)、[后端交接](HANDOFF-BACKEND-DESIGN-2026-09-11.md)、[创建契约](../../../../current/issue-page-create-api-contract.md)及[首批浏览器契约](../../../../current/first-batch-browser-api-contract.md)整理。当前交接仍标工程骨架，业务API／持久化／运行接入未完成；本次没有重新执行源码或运行验收，不把文档成熟度当实现完成度。

本表由页面独占维护；后端核依赖与边界。收发原文、轮次、TODO和用户采用范围记各自09-11日志，通信机制沿[约定](design-communication.md)。

依赖核对：后端已确认本表r1及一次落盘核对通过，1／3，无剩余分歧，原文见双方09-11日志（页面CP11／后端CP09）。这仅是文档依赖核对；6项具体范围、分批和暂缓方向现已获用户确认并开始设计；开发仍单独进行，不扩大为未定协议已采用。
