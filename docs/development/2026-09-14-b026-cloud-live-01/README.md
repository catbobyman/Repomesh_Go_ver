# B02.6 Cloud VM 真实验收 01

账号 A 在 `https://127.0.0.1:18443/` 上的新 App 轮次。进度只改 [CHECKLIST.md](CHECKLIST.md)。

模板副本是 [live-acceptance.md](live-acceptance.md)。只读快照由 `helpers/live-snapshot.sh` 写入 `snapshots/`。决策行在 [decisions.tsv](decisions.tsv)。

本轮不覆盖 2026-09-12 的 LIVE 目录。本轮不把 B02 标成 `VERIFIED`。
