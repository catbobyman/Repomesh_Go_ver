# 专用安装准备

当前 GitHub 登录账号为 catbobyman，显示名 catmem，数字 ID 137759882。专用 App 的 Install App 页面存在安装入口，尚未安装。实际安装授权页显示 Read access to metadata，以及 Read and write access to code and pull requests，与本批要求相符。

用户未提供现有专用私仓选择，代理在等待可选输入期间按已说明的默认方案准备并创建了专用私有空仓。稳定 GitHub 仓库 ID 为 1367444901；页面 Private 标记已实际确认。未向该仓库提交产品代码，没有创建公开仓库。创建前检查私有选项、仓库名和 owner；首次控制脚本按按钮文本而非完整可访问名称检查 owner，守卫退出，未创建。检查真实可访问名称 `catbobyman, Owner (Required)` 后才提交创建。

App 安装范围准备为 Only select repositories，只选择该专用仓库。账号持有人的 Install 同意尚待执行，不能据表单准备声称安装已完成。错误账号重连与读权撤回所需的第二个测试账号也仍待账号持有人提供。

安装表单已实际验证 `Only select repositories=true`，`repository_ids[]` 仅有一个值 1367444901，Install 按钮可用。没有点击 Install。表单位于 Windows Chrome target `BB9E3828289E2C77B28BF717352CF7F8`，路径 `/apps/repomesh-bohan-b026/installations/new/permissions`。页面已切到前台并临时置顶后恢复普通层级。

RepoMesh 主标签 target 仍为 `CBEA86CD7C41D085994D442CEB5B60A4`，停在同账号重连成功结果页；监控 03 持续记录该标签，不覆盖 GitHub 安装表单。专用仓库标签 target 为 `391D461D84C56BBCDDF0BBE6866E95B4`。不要按 page 0 假设用途，也不要关闭原验收窗口。
