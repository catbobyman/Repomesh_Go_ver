# Account B collaborator invitation helper check

- API resolution: Windows Node reads the fixed private config and key through UNC paths, signs the App JWT in memory, validates the App and A installation, creates one ephemeral installation token, and requires successful revocation before any UI mutation. The unique repository must be ID `1367444901`, owned by account A, with the fixed selected-installation permissions.
- Live read-only diagnosis: API validation and token revocation passed. The repository access page validated account A and repository ID; target ID was `401F8A47E432AFFD87512CD38032284C`. Account B was absent and exactly one visible `Add people` button was present. No invitation control was clicked or filled.
- Resume diagnosis: the first guarded run opened the dialog but stopped before filling or submission because the old search labels did not match. Target `89BE0DE153AC22A3CB4B7DE41A035BB0` has exactly one visible `combobox` whose accessible name and placeholder are fixed `Find people` and whose name attribute is `member`. The tool now resumes that unique open dialog; only when no such dialog exists does it require B absence and click `Add people`.
- Post-submit recovery diagnosis: after the second guarded run returned `PENDING_STATE_NOT_CONFIRMED`, a bounded read-only CDP evaluation on target `89BE0DE153AC22A3CB4B7DE41A035BB0` revalidated account A and repository ID. It found one exact visible B login whose enclosing fixed row variant has both pending/invited semantics and a remove/cancel/revoke control. This proves `INVITE_PENDING`. The tool now recognizes that state before opening or filling invitation controls and exclusively writes the safe evidence without submitting again.
- Mutation guards: the tool fills only one fixed search textbox with account B, accepts one exact B candidate, validates stable B ID `328458192` when exposed, and clicks one exact final invitation variant. Password, 2FA, sudo, ambiguous controls, or missing pending state return `NEEDS_HUMAN` with fixed safe fields.
- Evidence: the caller supplies a new absolute Windows/UNC evidence path. Success creates it exclusively as 0600 after the fixed pending/invited UI state is observed. Output never contains a repository name or URL, JWT, token, config/key content, DOM, cookie, query, raw response, or raw error.
- Static validation: `node --check docs/development/2026-09-12-b026-second-account-02/invite-b-collaborator.mjs` passed on 2026-09-12.
- Runtime mutation validation: the prior caller run clicked the final invite control and stopped at its conservative postcondition. This follow-up performed read-only CDP diagnosis only; it did not click, fill, submit, navigate, or repeat the invitation.
