# 独立复核与修订

复核者 gpt-5.6-sol 只读检查验证脚本和迁移脚本，未参与主实现。

1. 验证脚本的平台后缀、npm 入口及清理目录比较未发现实质问题。随后 Windows 和 Linux B01 均通过。
2. 复制脚本最初用原始 Git index 字节判断暂存一致性，可能误判 stat cache 刷新。已改用 ls-files --stage 的逻辑内容 SHA256，并关闭可选索引刷新。
3. 最初统一 0644 会损坏 AgentTeams 的可执行脚本。已按两个仓库的索引恢复模式，开启 core.filemode，并在最终检查中逐项核对。
4. 复核要求适配后再检查工作区与实际模式。迁移脚本已补足根仓库和上游的最终检查；本轮实际结果另由 final-verification.json 核对，不能把事后修改的复制脚本说成本轮重新执行过。
5. 复制遇到历史文件读权限及重试中的既有链接。使用 WSL root 读取历史文件，目标归用户所有；既有链接必须与源目标一致。

过程中的一次重复打包尝试被已有产物保护拒绝，未覆盖产物。最终产物由成功的打包调用生成，并另行核对哈希和运行行为。

WSL 配置原文件没有末尾换行，首次追加使两个设置连在同一行。已根据备份重新分行写入并重启，仅保留 NAT 和 localhostForwarding=true；最终 Windows 浏览器与 HTTP 访问重新通过，见 network-verification.json。
