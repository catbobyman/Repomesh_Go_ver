# 工作区同步记录

2026-09-13。用户要求编写交接与 Prompt 后同步 GitHub，并明确选择“同步当前工作区全部项目改动（排除秘密及本机配置）”。目标为 origin 的 main；同步前 HEAD 为 ad9a49b2fe3b5fe743a65c838ded6cb022e79363。

纳入当前 B02／B03 产品源码、前端、迁移、示例配置、工程脚本、AGENTS／README、现行设计文档及可发布历史证据、本轮 B04—B06 设计、交接和 Prompt。本次不改产品行为，不将 B04—B06 设计标记为采用或实现。排除本机 .codex/config.toml、真实秘密配置、依赖／构建产物和 Git 已忽略的本地资料。

原设计包完整保留，新的导航与同步检查不回写历史 checks／manifest。旧文档中禁止提交推送的语句描述当时任务边界，本次 Git 操作由新的明确用户授权覆盖。推送结果以实际远端 SHA 为准，不以本文件尚未执行时的计划作为成功证据。

## 发布前检查

- `go build ./...`、`go test -json ./...`、`go vet ./...` 均退出 0。测试事件为 167 PASS、57 SKIP；其中包含子测试，不能与历史 PostgreSQL 73 项直接比较。未设置专用数据库／浏览器 fixture，真实 PostgreSQL、浏览器服务及非当前权限环境用例按现有条件跳过，没有算作通过。
- `npm --prefix web ci`、`run typecheck`、`test`、`run build` 均退出 0；前端 28 PASS、0 SKIP。构建产物和依赖继续由 Git 忽略。
- 没有重跑数据库／浏览器／配套发布／race／真实 GitHub 或模型验收；B03 历史末轮证据保持，B04—B06 运行验收仍 NOT_RUN。
- 文本检查覆盖拟发布的改动及原设计 ZIP 内容，命中项已核对为协议占位、受控测试值或脚本变量，未发现真实私钥／GitHub token／模型 Key。历史 JSON 字段检查未发现 token、密码或 Cookie 正文；36 张图片来自 B02 合成浏览器夹具，核对生成脚本并抽查最终轮全部 6 种页面。
- 远端 main 实测为 `4516805a276d65eb79490a12783654f3d7b1c677`；本地已有、尚未推送的 `ad9a49b` 是其后继，随本次一并同步。额外检查该提交的 405 个变更文件及 ZIP 成员，未命中真实私钥／GitHub token／模型 Key。

`.codex/config.toml` 的当前本机字节保持，只停止 Git 跟踪并新增忽略规则；发布 `.codex/config.example.toml` 占位模板和使用说明。原 `ad9a49b` 历史中的旧 Windows 路径配置为非秘密项目配置，保留已有提交历史，不改写历史提交；同步后的 main 文件树不含本机配置。

最终暂存清单、文档链接与设计包原字节核对另见 checks.json。该记录证明提交前检查；最终推送成功须再比较远端 SHA 与本地 HEAD。

原始证据与设计清单中的现行文档在 `.gitattributes` 中禁用换行转换，保证这次暂存的字节可与其哈希记录比较；没有改写旧证据。空的 WSL `stop-runtime` 本机控制标记保留本地并忽略。使用识别 CRLF 的空白检查时，产品／现行文档及本次新交接均通过；全量暂存检查返回 2，剩余 122 处均在历史输出、旧说明或原始 changes.diff 中，按原字节保留，不称全量空白检查通过。
